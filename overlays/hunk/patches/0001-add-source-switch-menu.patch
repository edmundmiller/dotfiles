diff --git a/src/ui/App.tsx b/src/ui/App.tsx
index 9c5cdd1..a754053 100644
--- a/src/ui/App.tsx
+++ b/src/ui/App.tsx
@@ -106,6 +106,14 @@ import { setMouseCapture } from "./lib/mouseCapture";
 import { openSelectedFileInEditor } from "./lib/openInEditor";
 import { resolveResponsiveLayout } from "./lib/responsive";
 import type { WorkspaceRefreshRequest } from "./currentReviewRefresh";
+import {
+  buildReviewSourceItems,
+  repoPathForInput,
+  resolveReviewSourceInput,
+  reviewSourceLabel,
+  sourceIndicatorLabel,
+  type ReviewSourceId,
+} from "./lib/sourceSwitch";
 
 type FocusArea = "files" | "filter" | "note";
 
@@ -850,6 +858,19 @@ export function App({
     setShowMenuBar((current) => !current);
   };
 
+  const currentViewOptions = useMemo(
+    () => ({
+      layoutMode,
+      themeId,
+      showAgentNotes,
+      showHunkHeaders,
+      showLineNumbers,
+      showMenuBar,
+      wrapLines,
+    }),
+    [layoutMode, showAgentNotes, showHunkHeaders, showLineNumbers, showMenuBar, themeId, wrapLines],
+  );
+
   const { canRefreshCurrentInput, refreshCurrentInput, triggerRefreshCurrentInput } =
     useCurrentReviewRefreshController({
       input: bootstrap.input,
@@ -858,18 +879,48 @@ export function App({
       onWatchReloadPending: publishWatchReloadPending,
       reloadContext: bootstrap.reloadContext,
       sourceLabel: bootstrap.changeset.sourceLabel,
-      view: {
-        layoutMode,
-        themeId,
-        showAgentNotes,
-        showHunkHeaders,
-        showLineNumbers,
-        showMenuBar,
-        wrapLines,
-      },
+      view: currentViewOptions,
       watchRuntime,
     });
 
+  const gitBackedRepoPath = repoPathForInput(bootstrap.input, bootstrap.changeset.sourceLabel);
+  const [reviewSourceRepoPath, setReviewSourceRepoPath] = useState(gitBackedRepoPath);
+  const repoPath = gitBackedRepoPath ?? reviewSourceRepoPath;
+  const computedSourceLabel = sourceIndicatorLabel(
+    bootstrap.input,
+    bootstrap.changeset.sourceLabel,
+  );
+  const [selectedSourceLabel, setSelectedSourceLabel] = useState(computedSourceLabel);
+  const reviewSourceItems = useMemo(
+    () => buildReviewSourceItems(bootstrap.input, repoPath),
+    [bootstrap.input, repoPath],
+  );
+
+  const switchReviewSource = useCallback(
+    (sourceId: ReviewSourceId) => {
+      const result = resolveReviewSourceInput(sourceId, repoPath, currentViewOptions);
+      if (result.kind === "notice") {
+        showSessionNotice(result.message);
+        return;
+      }
+
+      if (result.sourcePath) setReviewSourceRepoPath(result.sourcePath);
+      void onReloadSession(result.input, {
+        resetApp: true,
+        sourcePath: result.sourcePath,
+      })
+        .then(() => {
+          setSelectedSourceLabel(result.label);
+          showTransientNotice(`Source: ${result.label}`);
+        })
+        .catch((error) => {
+          console.error("Failed to switch Hunk source.", error);
+          showSessionNotice(`Failed to switch source: ${reviewSourceLabel(sourceId)}`);
+        });
+    },
+    [currentViewOptions, onReloadSession, repoPath, showSessionNotice, showTransientNotice],
+  );
+
   const {
     closeExtensionTrustPrompt,
     denyRepoExtensions,
@@ -1091,6 +1142,8 @@ export function App({
           action: exitKeyboardMode,
         }
       : undefined,
+    reviewSourceItems,
+    switchReviewSource,
     copyDecorations,
     layoutMode,
     filesPaneVisible,
@@ -1107,6 +1160,7 @@ export function App({
     activeMenuId,
     activeMenuItemIndex,
     activeMenuSpec,
+    activateMenuItemByHint,
     activeMenuWidth,
     activateCurrentMenuItem,
     closeMenu,
@@ -1119,6 +1173,7 @@ export function App({
   } = useMenuController(menus);
 
   useAppKeyboardShortcuts({
+    activateMenuItemByHint,
     activeMenuId,
     activateCurrentMenuItem,
     closeAgentSkill,
@@ -1146,6 +1201,7 @@ export function App({
     moveMenuItem,
     moveThemeSelector,
     openMenu,
+    openSourceSelector: () => openMenu("source"),
     saveConfigPromptOpen,
     saveViewPreferencesAndQuit,
     discardViewPreferencesAndQuit,
@@ -1169,7 +1225,8 @@ export function App({
     (sum, file) => sum + file.stats.deletions,
     0,
   );
-  const topTitle = `${bootstrap.changeset.title}  ${changedFileCount} ${changedFileLabel}  +${totalAdditions}  -${totalDeletions}`;
+  const sourceLabel = computedSourceLabel ?? selectedSourceLabel;
+  const topTitle = `${sourceLabel ? `${sourceLabel} · ` : ""}${bootstrap.changeset.title}  ${changedFileCount} ${changedFileLabel}  +${totalAdditions}  -${totalDeletions}`;
   const diffHeaderStatsWidth = maxFileHeaderStatsWidth(filteredFiles);
   const diffHeaderLabelWidth = Math.max(0, diffContentWidth - diffHeaderStatsWidth - 1);
   const diffSeparatorWidth = Math.max(0, diffContentWidth - 2);
diff --git a/src/ui/AppHost.interactions.test.tsx b/src/ui/AppHost.interactions.test.tsx
index e7c7074..f53a9eb 100644
--- a/src/ui/AppHost.interactions.test.tsx
+++ b/src/ui/AppHost.interactions.test.tsx
@@ -789,7 +789,7 @@ describe("App interactions", () => {
       await flush(setup);
 
       let frame = setup.captureCharFrame();
-      expect(frame).toContain("File  View  Navigate  Agent  Help");
+      expect(frame).toContain("File  Source  View  Navigate  Agent  Help");
 
       await act(async () => {
         await setup.mockInput.pressKey("m", { shift: true });
@@ -797,7 +797,7 @@ describe("App interactions", () => {
       await flush(setup);
 
       frame = setup.captureCharFrame();
-      expect(frame).not.toContain("File  View  Navigate  Agent  Help");
+      expect(frame).not.toContain("File  Source  View  Navigate  Agent  Help");
       expect(firstNonEmptyLine(frame)).not.toContain("─");
 
       await act(async () => {
@@ -807,7 +807,7 @@ describe("App interactions", () => {
         nextFrame.includes("Toggle files/filter focus"),
       );
       expect(frame).toContain("Toggle files/filter focus");
-      expect(frame).not.toContain("File  View  Navigate  Agent  Help");
+      expect(frame).not.toContain("File  Source  View  Navigate  Agent  Help");
     } finally {
       await act(async () => {
         setup.renderer.destroy();
@@ -828,7 +828,7 @@ describe("App interactions", () => {
       await flush(setup);
 
       let frame = setup.captureCharFrame();
-      expect(frame).not.toContain("File  View  Navigate  Agent  Help");
+      expect(frame).not.toContain("File  Source  View  Navigate  Agent  Help");
       expect(firstNonEmptyLine(frame)).not.toContain("─");
 
       await act(async () => {
@@ -838,7 +838,7 @@ describe("App interactions", () => {
         nextFrame.includes("Toggle files/filter focus"),
       );
       expect(frame).toContain("Toggle files/filter focus");
-      expect(frame).not.toContain("File  View  Navigate  Agent  Help");
+      expect(frame).not.toContain("File  Source  View  Navigate  Agent  Help");
     } finally {
       await act(async () => {
         setup.renderer.destroy();
@@ -1617,6 +1617,10 @@ describe("App interactions", () => {
       expect(frame).toContain("Reload");
       expect(frame).toContain("Quit");
 
+      await act(async () => {
+        await setup.mockInput.pressArrow("right");
+      });
+      await flush(setup);
       await act(async () => {
         await setup.mockInput.pressArrow("right");
       });
@@ -1941,7 +1945,7 @@ describe("App interactions", () => {
       });
       await waitForFrame(setup, (frame) => frame.includes("Toggle files/filter focus"), 12);
 
-      for (let index = 0; index < 3; index += 1) {
+      for (let index = 0; index < 4; index += 1) {
         await act(async () => {
           await setup.mockInput.pressArrow("right");
         });
@@ -3228,7 +3232,7 @@ describe("App interactions", () => {
       await flush(setup);
 
       let frame = setup.captureCharFrame();
-      expect(frame).not.toContain("File  View  Navigate  Agent  Help");
+      expect(frame).not.toContain("File  Source  View  Navigate  Agent  Help");
       expect((frame.match(/alpha\.ts/g) ?? []).length).toBe(1);
 
       await act(async () => {
@@ -3237,7 +3241,7 @@ describe("App interactions", () => {
       await flush(setup);
 
       frame = setup.captureCharFrame();
-      expect(frame).not.toContain("File  View  Navigate  Agent  Help");
+      expect(frame).not.toContain("File  Source  View  Navigate  Agent  Help");
       expect((frame.match(/alpha\.ts/g) ?? []).length).toBe(2);
 
       await act(async () => {
diff --git a/src/ui/AppHost.reload.test.tsx b/src/ui/AppHost.reload.test.tsx
index eafe0f2..2dcba00 100644
--- a/src/ui/AppHost.reload.test.tsx
+++ b/src/ui/AppHost.reload.test.tsx
@@ -453,4 +453,69 @@ describe("reload agent attention marks", () => {
       await removeTestDirectory(dir);
     }
   });
+
+  test("F9 source menu mnemonics switch sources without stealing normal app keys", async () => {
+    const dir = mkdtempSync(join(tmpdir(), "hunk-source-switch-git-"));
+    const file = join(dir, "test.txt");
+
+    execSync("git init && git config user.email test@test && git config user.name test", {
+      cwd: dir,
+      stdio: "ignore",
+    });
+    writeFileSync(file, "base\n");
+    execSync("git add . && git commit -m init", { cwd: dir, stdio: "ignore" });
+
+    writeFileSync(file, "base\nstaged change\n");
+    execSync("git add .", { cwd: dir, stdio: "ignore" });
+    writeFileSync(file, "base\nstaged change\nunstaged change\n");
+
+    const bootstrap = await loadAppBootstrap(
+      { kind: "vcs", staged: false, options: { mode: "stack", excludeUntracked: true } },
+      { cwd: dir, vcsCatalog: getBundledVcsCatalog() },
+    );
+
+    const setup = await testRender(<AppHost bootstrap={bootstrap} />, {
+      width: 140,
+      height: 20,
+    });
+
+    try {
+      await settleHighlights(setup);
+      expect(setup.captureCharFrame()).toContain("unstaged change");
+
+      await act(async () => {
+        await setup.mockInput.typeText("s");
+      });
+      await flush(setup);
+      expect(setup.captureCharFrame()).toContain("unstaged change");
+
+      await act(async () => {
+        await setup.mockInput.pressKey("F9");
+      });
+      await flush(setup);
+      expect(setup.captureCharFrame()).toContain("Staged");
+
+      await act(async () => {
+        await setup.mockInput.typeText("s");
+      });
+
+      let switched = false;
+      for (let attempt = 0; attempt < 30; attempt++) {
+        await flush(setup);
+        const frame = setup.captureCharFrame();
+        if (frame.includes("staged change") && !frame.includes("unstaged change")) {
+          switched = true;
+          break;
+        }
+        await Bun.sleep(50);
+      }
+
+      expect(switched).toBe(true);
+    } finally {
+      await act(async () => {
+        setup.renderer.destroy();
+      });
+      await removeTestDirectory(dir);
+    }
+  });
 });
diff --git a/src/ui/AppHost.responsive.test.tsx b/src/ui/AppHost.responsive.test.tsx
index 24d688d..5be3216 100644
--- a/src/ui/AppHost.responsive.test.tsx
+++ b/src/ui/AppHost.responsive.test.tsx
@@ -165,6 +165,12 @@ describe("responsive app", () => {
       await act(async () => {
         await setup.renderOnce();
       });
+      await act(async () => {
+        await setup.mockInput.pressArrow("right");
+      });
+      await act(async () => {
+        await setup.renderOnce();
+      });
 
       const menuFrame = setup.captureCharFrame();
       expect(menuFrame).toContain("[x] Files pane");
@@ -224,12 +230,12 @@ describe("responsive app", () => {
     const narrow = await captureFrameForBootstrap(createBootstrap("auto", true), 150);
     const tight = await captureFrameForBootstrap(createBootstrap("auto", true), 110);
 
-    expect(wide).not.toContain("File  View  Navigate  Agent  Help");
+    expect(wide).not.toContain("File  Source  View  Navigate  Agent  Help");
     expect(wide).not.toContain("F10 menu");
     expect((wide.match(/alpha\.ts/g) ?? []).length).toBe(1);
     expect(wide).toMatch(/▌.*▌/);
 
-    expect(narrow).not.toContain("File  View  Navigate  Agent  Help");
+    expect(narrow).not.toContain("File  Source  View  Navigate  Agent  Help");
     expect(narrow).not.toContain("F10 menu");
     expect((narrow.match(/alpha\.ts/g) ?? []).length).toBe(1);
     expect(narrow).toMatch(/▌.*▌/);
diff --git a/src/ui/components/chrome/menu.ts b/src/ui/components/chrome/menu.ts
index 952aff7..5a545ad 100644
--- a/src/ui/components/chrome/menu.ts
+++ b/src/ui/components/chrome/menu.ts
@@ -1,6 +1,6 @@
 import { measureTextWidth } from "../../lib/text";
 
-export type MenuId = "file" | "view" | "navigate" | "agent" | "extensions" | "help";
+export type MenuId = "file" | "source" | "view" | "navigate" | "agent" | "extensions" | "help";
 
 export type MenuEntry =
   | {
@@ -34,6 +34,7 @@ export type AppMenus = Partial<Record<MenuId, MenuEntry[]>>;
 
 const MENU_LABELS: Record<MenuId, string> = {
   file: "File",
+  source: "Source",
   view: "View",
   navigate: "Navigate",
   agent: "Agent",
diff --git a/src/ui/components/ui-components.test.tsx b/src/ui/components/ui-components.test.tsx
index 74d9acd..839ecd4 100644
--- a/src/ui/components/ui-components.test.tsx
+++ b/src/ui/components/ui-components.test.tsx
@@ -4537,7 +4537,7 @@ describe("UI components", () => {
     const bootstrap = createBootstrap();
     const frame = await captureFrame(<AppHost bootstrap={bootstrap} />, 280, 24);
 
-    expect(frame).toContain("File  View  Navigate  Agent  Help");
+    expect(frame).toContain("File  Source  View  Navigate  Agent  Help");
     expect(frame).toContain("alpha.ts");
     expect(frame).toContain("beta.ts");
     expect(frame).toContain("@@ -1,1 +1,2 @@");
diff --git a/src/ui/hooks/useAppKeyboardShortcuts.ts b/src/ui/hooks/useAppKeyboardShortcuts.ts
index 23327df..4b410f1 100644
--- a/src/ui/hooks/useAppKeyboardShortcuts.ts
+++ b/src/ui/hooks/useAppKeyboardShortcuts.ts
@@ -23,6 +23,7 @@ type FocusArea = "files" | "filter" | "note";
 export interface UseAppKeyboardShortcutsOptions {
   activeMenuId: MenuId | null;
   activateCurrentMenuItem: () => void;
+  activateMenuItemByHint: (hint: string) => boolean;
   closeAgentSkill: () => void;
   closeHelp: () => void;
   closeMenu: () => void;
@@ -63,6 +64,7 @@ export interface UseAppKeyboardShortcutsOptions {
   moveMenuItem: (delta: number) => void;
   moveThemeSelector: (delta: number) => void;
   openMenu: (menuId: MenuId) => void;
+  openSourceSelector: () => void;
   saveConfigPromptOpen: boolean;
   saveViewPreferencesAndQuit: () => void;
   discardViewPreferencesAndQuit: () => void;
@@ -96,6 +98,7 @@ export interface UseAppKeyboardShortcutsOptions {
  * full contract, including why a boolean cannot express it.
  */
 export function useAppKeyboardShortcuts({
+  activateMenuItemByHint,
   activeMenuId,
   activateCurrentMenuItem,
   closeAgentSkill,
@@ -121,6 +124,7 @@ export function useAppKeyboardShortcuts({
   sendKeyboardModeKey,
   focusArea,
   moveMenuItem,
+  openSourceSelector,
   moveThemeSelector,
   openMenu,
   saveConfigPromptOpen,
@@ -230,6 +234,14 @@ export function useAppKeyboardShortcuts({
     return "mine";
   };
 
+  /** F9 opens the source menu without affecting normal app shortcuts. */
+  const handleSourceSelectorShortcut = (key: KeyEvent): KeyOwner => {
+    if (key.name !== "f9") return "notMine";
+    if (focusAreaRef.current === "note") return "mine";
+    openSourceSelector();
+    return "mine";
+  };
+
   /** Escape closes the topmost open overlay (agent skill, then help). */
   const handleDialogShortcut = (key: KeyEvent): KeyOwner => {
     if (!isEscapeKey(key)) {
@@ -461,6 +473,10 @@ export function useAppKeyboardShortcuts({
       return "mine";
     }
 
+    if ((key.sequence || key.name) && activateMenuItemByHint(key.sequence ?? key.name)) {
+      return "mine";
+    }
+
     return "notMine";
   };
 
@@ -598,6 +614,7 @@ export function useAppKeyboardShortcuts({
         handleExtensionTrustPromptShortcut,
         handleSaveConfigPromptShortcut,
         handleExtensionDialogShortcut,
+        handleSourceSelectorShortcut,
         handleMenuToggleShortcut,
         handleDialogShortcut,
         handleThemeSelectorShortcut,
diff --git a/src/ui/hooks/useMenuController.ts b/src/ui/hooks/useMenuController.ts
index c1c9494..e8a7e26 100644
--- a/src/ui/hooks/useMenuController.ts
+++ b/src/ui/hooks/useMenuController.ts
@@ -107,6 +107,18 @@ export function useMenuController(menus: AppMenus) {
     closeMenu();
   };
 
+  const activateMenuItemByHint = (hint: string) => {
+    if (!openMenuId) return false;
+    const normalizedHint = hint.toLowerCase();
+    const entry = activeMenuEntries.find(
+      (item) => item.kind === "item" && item.hint?.toLowerCase() === normalizedHint,
+    );
+    if (!entry || entry.kind !== "item") return false;
+    entry.action();
+    closeMenu();
+    return true;
+  };
+
   const activeMenuSpec = menuSpecs.find((menu) => menu.id === openMenuId);
   const activeMenuWidth = menuWidth(activeMenuEntries) + 2;
 
@@ -117,6 +129,7 @@ export function useMenuController(menus: AppMenus) {
     activeMenuSpec,
     activeMenuWidth,
     activateCurrentMenuItem,
+    activateMenuItemByHint,
     closeMenu,
     menuSpecs,
     moveMenuItem,
diff --git a/src/ui/lib/appCommands.test.ts b/src/ui/lib/appCommands.test.ts
index 7e0ddd0..3873fe0 100644
--- a/src/ui/lib/appCommands.test.ts
+++ b/src/ui/lib/appCommands.test.ts
@@ -447,6 +447,8 @@ describe("command catalog parity", () => {
     const { commands } = createTestCommands();
     const catalogued = new Set(APP_COMMAND_CATALOG.map((entry) => entry.id));
     const menus = buildAppMenus({
+      reviewSourceItems: [],
+      switchReviewSource: () => {},
       commands,
       copyDecorations: false,
       cursorLine: "row",
diff --git a/src/ui/lib/appMenus.test.ts b/src/ui/lib/appMenus.test.ts
index a738816..b73dcdb 100644
--- a/src/ui/lib/appMenus.test.ts
+++ b/src/ui/lib/appMenus.test.ts
@@ -25,6 +25,11 @@ const MENU_STATE: Omit<BuildAppMenusOptions, "commands" | "extensionCommands"> =
   showLineNumbers: true,
   showMenuBar: true,
   wrapLines: true,
+  reviewSourceItems: [
+    { id: "unstaged", label: "Unstaged", hint: "u", active: true, enabled: true },
+    { id: "staged", label: "Staged", hint: "s", active: false, enabled: true },
+  ],
+  switchReviewSource: () => {},
 };
 
 /** Build the built-in command table over recording callbacks, plus the log it writes. */
@@ -340,3 +345,21 @@ describe("the Extensions menu", () => {
     expect(ran).toEqual(["notes", "blame"]);
   });
 });
+
+describe("the Source menu", () => {
+  test("marks the active source and dispatches selection", () => {
+    const selected: string[] = [];
+    const menus = buildAppMenus({
+      commands: createTestCommands().commands,
+      ...MENU_STATE,
+      switchReviewSource: (source) => selected.push(source),
+    });
+
+    expect(items(menus.source)).toMatchObject([
+      { label: "Unstaged", hint: "u", checked: true },
+      { label: "Staged", hint: "s", checked: false },
+    ]);
+    items(menus.source)[1]?.action();
+    expect(selected).toEqual(["staged"]);
+  });
+});
diff --git a/src/ui/lib/appMenus.ts b/src/ui/lib/appMenus.ts
index 0614b6c..4fa8cbf 100644
--- a/src/ui/lib/appMenus.ts
+++ b/src/ui/lib/appMenus.ts
@@ -1,5 +1,6 @@
 import type { CursorLine, LayoutMode } from "../../core/run/commandInputs";
 import type { AppMenus, MenuEntry, MenuId } from "../components/chrome/menu";
+import type { ReviewSourceId, ReviewSourceItem } from "./sourceSwitch";
 import { executeAppCommand, isCommandEnabled, type AppCommand } from "./appCommands";
 
 /**
@@ -30,6 +31,8 @@ export interface BuildAppMenusOptions {
   commands: readonly AppCommand[];
   /** The extension-contributed subset, in registration order, for the Extensions menu. */
   extensionCommands?: readonly AppCommand[];
+  reviewSourceItems: readonly ReviewSourceItem[];
+  switchReviewSource: (sourceId: ReviewSourceId) => void;
   /** Host-owned per-file presentation choices appended to View. */
   fileViewEntries?: readonly MenuEntry[];
   /** Host-owned escape hatch shown while a session keyboard mode is active. */
@@ -126,6 +129,8 @@ export function buildAppMenus({
   fileViewEntries = [],
   fileViewApplyAllLabel,
   keyboardModeExitEntry,
+  reviewSourceItems,
+  switchReviewSource,
   copyDecorations,
   cursorLine,
   layoutMode,
@@ -137,7 +142,7 @@ export function buildAppMenus({
   showMenuBar,
   wrapLines,
 }: BuildAppMenusOptions): AppMenus {
-  const specs: Record<Exclude<MenuId, "extensions">, MenuEntrySpec[]> = {
+  const specs: Record<Exclude<MenuId, "extensions" | "source">, MenuEntrySpec[]> = {
     file: [
       { commandId: "hunk.app.toggleFocusArea", label: "Toggle files/filter focus" },
       { commandId: "hunk.review.focusFilter", label: "Focus filter" },
@@ -232,6 +237,13 @@ export function buildAppMenus({
 
   return {
     file: toMenuEntries(commands, specs.file),
+    source: reviewSourceItems.map((item) => ({
+      kind: "item",
+      label: item.label,
+      hint: item.hint,
+      checked: item.active,
+      action: () => switchReviewSource(item.id),
+    })),
     view: [
       ...toMenuEntries(commands, specs.view),
       ...fileViewEntries,
diff --git a/src/ui/lib/sourceSwitch.test.ts b/src/ui/lib/sourceSwitch.test.ts
new file mode 100644
index 0000000..5dd08f1
--- /dev/null
+++ b/src/ui/lib/sourceSwitch.test.ts
@@ -0,0 +1,282 @@
+import { execFileSync } from "node:child_process";
+import {
+  chmodSync,
+  mkdirSync,
+  mkdtempSync,
+  readFileSync,
+  rmSync,
+  utimesSync,
+  writeFileSync,
+} from "node:fs";
+import { tmpdir } from "node:os";
+import { dirname, join } from "node:path";
+import { describe, expect, test } from "bun:test";
+import {
+  activeReviewSource,
+  buildReviewSourceItems,
+  resolveReviewSourceInput,
+  repoPathForInput,
+  sourceIndicatorLabel,
+} from "./sourceSwitch";
+import type { CliInput } from "../../core/run/commandInputs";
+
+function git(repo: string, args: string[]) {
+  return execFileSync("git", args, {
+    cwd: repo,
+    encoding: "utf8",
+    stdio: ["ignore", "pipe", "ignore"],
+  }).trim();
+}
+
+function createRepo() {
+  const repo = mkdtempSync(join(tmpdir(), "hunk-source-switch-"));
+  git(repo, ["init"]);
+  git(repo, ["config", "user.email", "test@test"]);
+  git(repo, ["config", "user.name", "Test"]);
+  writeFileSync(join(repo, "file.txt"), "base\n");
+  git(repo, ["add", "."]);
+  git(repo, ["commit", "-m", "init"]);
+  return repo;
+}
+
+describe("source switch", () => {
+  test("builds source entries with active staged source", () => {
+    const input: CliInput = { kind: "vcs", staged: true, options: {} };
+    const items = buildReviewSourceItems(input);
+
+    expect(items.map((item) => item.id)).toEqual([
+      "unstaged",
+      "staged",
+      "commit",
+      "branch",
+      "last-agent-turn",
+    ]);
+    expect(items.find((item) => item.id === "staged")?.active).toBe(true);
+    expect(activeReviewSource(input)).toBe("staged");
+  });
+
+  test("resolves built-in sources to cli inputs", () => {
+    const options = { mode: "stack" as const };
+
+    expect(resolveReviewSourceInput("unstaged", "/repo", options)).toMatchObject({
+      kind: "input",
+      input: { kind: "vcs", staged: false, options },
+      sourcePath: "/repo",
+    });
+    expect(resolveReviewSourceInput("staged", "/repo", options)).toMatchObject({
+      kind: "input",
+      input: { kind: "vcs", staged: true, options },
+      sourcePath: "/repo",
+    });
+    expect(resolveReviewSourceInput("commit", "/repo", options)).toMatchObject({
+      kind: "input",
+      input: { kind: "show", ref: "HEAD", options },
+      sourcePath: "/repo",
+    });
+  });
+
+  test("resolves branch source through upstream merge-base", () => {
+    const repo = createRepo();
+    const remote = mkdtempSync(join(tmpdir(), "hunk-source-remote-"));
+    git(remote, ["init", "--bare"]);
+
+    try {
+      git(repo, ["branch", "-M", "main"]);
+      git(repo, ["remote", "add", "origin", remote]);
+      git(repo, ["push", "-u", "origin", "main"]);
+      writeFileSync(join(repo, "file.txt"), "base\nbranch\n");
+      git(repo, ["add", "."]);
+      git(repo, ["commit", "-m", "branch"]);
+
+      const result = resolveReviewSourceInput("branch", repo, {});
+      expect(result.kind).toBe("input");
+      if (result.kind === "input") {
+        expect(result.input).toMatchObject({ kind: "vcs", staged: false });
+        expect(result.input.kind === "vcs" ? result.input.range : "").toEndWith("...HEAD");
+      }
+    } finally {
+      rmSync(repo, { force: true, recursive: true });
+      rmSync(remote, { force: true, recursive: true });
+    }
+  });
+
+  test("missing last-agent marker returns a notice", () => {
+    const repo = createRepo();
+    try {
+      expect(resolveReviewSourceInput("last-agent-turn", repo, {})).toEqual({
+        kind: "notice",
+        message: "No Last agent turn source for this repo.",
+      });
+    } finally {
+      rmSync(repo, { force: true, recursive: true });
+    }
+  });
+
+  test("Pi marker resolves to patch input", () => {
+    const repo = createRepo();
+    try {
+      const markerPath = join(
+        repo,
+        git(repo, ["rev-parse", "--git-path", "hunk/last-pi-turn.json"]),
+      );
+      const patchPath = join(
+        repo,
+        git(repo, ["rev-parse", "--git-path", "hunk/last-pi-turn.patch"]),
+      );
+      mkdirSync(dirname(markerPath), { recursive: true });
+      writeFileSync(patchPath, "diff --git a/file.txt b/file.txt\n");
+      writeFileSync(markerPath, JSON.stringify({ version: 1, kind: "patch", file: patchPath }));
+
+      expect(resolveReviewSourceInput("last-agent-turn", repo, { mode: "split" })).toMatchObject({
+        kind: "input",
+        input: { kind: "patch", file: patchPath, options: { mode: "split" } },
+      });
+      expect(sourceIndicatorLabel({ kind: "patch", file: patchPath, options: {} }, "")).toBe(
+        "Last Pi turn",
+      );
+      expect(repoPathForInput({ kind: "patch", file: patchPath, options: {} }, "")).toBe(repo);
+      expect(activeReviewSource({ kind: "patch", file: patchPath, options: {} })).toBe(
+        "last-agent-turn",
+      );
+      expect(JSON.parse(readFileSync(markerPath, "utf8"))).toMatchObject({
+        kind: "patch",
+        file: patchPath,
+      });
+    } finally {
+      rmSync(repo, { force: true, recursive: true });
+    }
+  });
+
+  test("OMP marker resolves through same last-turn source seam", () => {
+    const repo = createRepo();
+    try {
+      const markerPath = join(
+        repo,
+        git(repo, ["rev-parse", "--git-path", "hunk/last-omp-turn.json"]),
+      );
+      const patchPath = join(
+        repo,
+        git(repo, ["rev-parse", "--git-path", "hunk/last-omp-turn.patch"]),
+      );
+      mkdirSync(dirname(markerPath), { recursive: true });
+      writeFileSync(patchPath, "diff --git a/file.txt b/file.txt\n");
+      writeFileSync(markerPath, JSON.stringify({ version: 1, kind: "patch", file: patchPath }));
+
+      const result = resolveReviewSourceInput("last-agent-turn", repo, { mode: "split" });
+      expect(result).toMatchObject({
+        kind: "input",
+        input: { kind: "patch", file: patchPath, options: { mode: "split" } },
+        label: "Last OMP turn",
+      });
+      expect(sourceIndicatorLabel({ kind: "patch", file: patchPath, options: {} }, "")).toBe(
+        "Last OMP turn",
+      );
+      expect(activeReviewSource({ kind: "patch", file: patchPath, options: {} })).toBe(
+        "last-agent-turn",
+      );
+      expect(repoPathForInput({ kind: "patch", file: patchPath, options: {} }, "")).toBe(repo);
+    } finally {
+      rmSync(repo, { force: true, recursive: true });
+    }
+  });
+
+  test("last-agent source uses the newest agent marker", () => {
+    const repo = createRepo();
+    try {
+      const piMarkerPath = join(
+        repo,
+        git(repo, ["rev-parse", "--git-path", "hunk/last-pi-turn.json"]),
+      );
+      const piPatchPath = join(
+        repo,
+        git(repo, ["rev-parse", "--git-path", "hunk/last-pi-turn.patch"]),
+      );
+      const ompMarkerPath = join(
+        repo,
+        git(repo, ["rev-parse", "--git-path", "hunk/last-omp-turn.json"]),
+      );
+      const ompPatchPath = join(
+        repo,
+        git(repo, ["rev-parse", "--git-path", "hunk/last-omp-turn.patch"]),
+      );
+      const codexMarkerPath = join(
+        repo,
+        git(repo, ["rev-parse", "--git-path", "hunk/last-codex-turn.json"]),
+      );
+      const codexPatchPath = join(
+        repo,
+        git(repo, ["rev-parse", "--git-path", "hunk/last-codex-turn.patch"]),
+      );
+      mkdirSync(dirname(piMarkerPath), { recursive: true });
+      writeFileSync(piPatchPath, "diff --git a/file.txt b/file.txt\n");
+      writeFileSync(ompPatchPath, "diff --git a/file.txt b/file.txt\n");
+      writeFileSync(codexPatchPath, "diff --git a/file.txt b/file.txt\n");
+      writeFileSync(piMarkerPath, JSON.stringify({ version: 1, kind: "patch", file: piPatchPath }));
+      writeFileSync(
+        ompMarkerPath,
+        JSON.stringify({ version: 1, kind: "patch", file: ompPatchPath }),
+      );
+      writeFileSync(
+        codexMarkerPath,
+        JSON.stringify({ version: 1, kind: "patch", file: codexPatchPath }),
+      );
+      utimesSync(piMarkerPath, new Date("2026-01-01T00:00:00Z"), new Date("2026-01-01T00:00:00Z"));
+      utimesSync(ompMarkerPath, new Date("2026-01-02T00:00:00Z"), new Date("2026-01-02T00:00:00Z"));
+      utimesSync(
+        codexMarkerPath,
+        new Date("2026-01-03T00:00:00Z"),
+        new Date("2026-01-03T00:00:00Z"),
+      );
+
+      const result = resolveReviewSourceInput("last-agent-turn", repo, {});
+      expect(result).toMatchObject({
+        kind: "input",
+        input: { kind: "patch", file: codexPatchPath },
+        label: "Last Codex turn",
+      });
+    } finally {
+      rmSync(repo, { force: true, recursive: true });
+    }
+  });
+  test("adds open PRs for the current branch and resolves PR diffs", () => {
+    const repo = createRepo();
+    const bin = mkdtempSync(join(tmpdir(), "hunk-source-gh-"));
+    const gh = join(bin, "gh");
+    const originalPath = process.env.PATH;
+
+    try {
+      git(repo, ["checkout", "-b", "feature"]);
+      writeFileSync(
+        gh,
+        `#!/bin/sh
+if [ "$2" = "diff" ]; then
+  printf 'diff --git a/file.txt b/file.txt\n--- a/file.txt\n+++ b/file.txt\n@@ -1 +1,2 @@\n base\n+pr change\n'
+else
+  printf '[{"number":123,"title":"Add source switch"}]'
+fi
+`,
+      );
+      chmodSync(gh, 0o755);
+      process.env.PATH = `${bin}:${originalPath ?? ""}`;
+
+      const items = buildReviewSourceItems({ kind: "vcs", staged: false, options: {} }, repo);
+      expect(items.find((item) => item.id === "pr:123")).toMatchObject({
+        label: "PR #123 Add source switch",
+        hint: "1",
+      });
+
+      const result = resolveReviewSourceInput("pr:123", repo, { mode: "stack" });
+      expect(result.kind).toBe("input");
+      if (result.kind === "input") {
+        expect(result).toMatchObject({ label: "PR #123", sourcePath: repo });
+        expect(result.input).toMatchObject({ kind: "patch", options: { mode: "stack" } });
+        expect(sourceIndicatorLabel(result.input, "")).toBe("PR #123");
+        expect(repoPathForInput(result.input, "")).toBe(repo);
+      }
+    } finally {
+      process.env.PATH = originalPath;
+      rmSync(repo, { force: true, recursive: true });
+      rmSync(bin, { force: true, recursive: true });
+    }
+  });
+});
diff --git a/src/ui/lib/sourceSwitch.ts b/src/ui/lib/sourceSwitch.ts
new file mode 100644
index 0000000..c1ef257
--- /dev/null
+++ b/src/ui/lib/sourceSwitch.ts
@@ -0,0 +1,447 @@
+import { execFileSync } from "node:child_process";
+import { existsSync, mkdirSync, readFileSync, statSync, writeFileSync } from "node:fs";
+import { basename, dirname, isAbsolute, join, resolve, sep } from "node:path";
+import type { CliInput, CommonOptions } from "../../core/run/commandInputs";
+
+export type BaseReviewSourceId = "unstaged" | "staged" | "commit" | "branch";
+export type LastAgentTurnReviewSourceId = "last-agent-turn";
+export type BuiltInReviewSourceId = BaseReviewSourceId | LastAgentTurnReviewSourceId;
+export type PullRequestReviewSourceId = `pr:${number}`;
+export type ReviewSourceId = BuiltInReviewSourceId | PullRequestReviewSourceId;
+
+export interface ReviewSourceItem {
+  id: ReviewSourceId;
+  label: string;
+  hint: string;
+  active: boolean;
+  enabled: boolean;
+}
+
+export type ReviewSourceSwitchResult =
+  | { kind: "input"; input: CliInput; sourcePath?: string; label: string }
+  | { kind: "notice"; message: string };
+
+interface BranchPullRequest {
+  number: number;
+  title: string;
+}
+
+interface AgentLastTurnSource {
+  label: string;
+  markerBasename: string;
+}
+
+const BASE_SOURCE_LABELS: Record<BaseReviewSourceId, string> = {
+  unstaged: "Unstaged",
+  staged: "Staged",
+  commit: "Commit",
+  branch: "Branch",
+};
+
+const BASE_SOURCE_HINTS: Record<BaseReviewSourceId, string> = {
+  unstaged: "u",
+  staged: "s",
+  commit: "c",
+  branch: "b",
+};
+
+const LAST_AGENT_TURN_SOURCE = {
+  id: "last-agent-turn",
+  label: "Last agent turn",
+  hint: "a",
+} as const;
+
+const AGENT_LAST_TURN_SOURCES: AgentLastTurnSource[] = [
+  { label: "Last Pi turn", markerBasename: "last-pi-turn" },
+  { label: "Last OMP turn", markerBasename: "last-omp-turn" },
+  { label: "Last Codex turn", markerBasename: "last-codex-turn" },
+];
+
+export const REVIEW_SOURCE_ORDER: BuiltInReviewSourceId[] = [
+  "unstaged",
+  "staged",
+  "commit",
+  "branch",
+  LAST_AGENT_TURN_SOURCE.id,
+];
+
+function run(repo: string, command: string, args: string[], timeout?: number) {
+  return execFileSync(command, args, {
+    cwd: repo,
+    env: process.env,
+    encoding: "utf8",
+    stdio: ["ignore", "pipe", "ignore"],
+    timeout,
+  }).trim();
+}
+
+function runGit(repo: string, args: string[]) {
+  return run(repo, "git", args);
+}
+
+function runGh(repo: string, args: string[], timeout = 30_000) {
+  return run(repo, "gh", args, timeout);
+}
+
+function agentLastTurnSourceByPatch(path: string) {
+  const patchBasename = basename(path).replace(/\.patch$/, "");
+  return AGENT_LAST_TURN_SOURCES.find((source) => source.markerBasename === patchBasename);
+}
+
+function sourceLabel(id: BuiltInReviewSourceId) {
+  if (id === LAST_AGENT_TURN_SOURCE.id) return LAST_AGENT_TURN_SOURCE.label;
+  return BASE_SOURCE_LABELS[id];
+}
+
+function sourceHint(id: BuiltInReviewSourceId) {
+  if (id === LAST_AGENT_TURN_SOURCE.id) return LAST_AGENT_TURN_SOURCE.hint;
+  return BASE_SOURCE_HINTS[id];
+}
+
+function parsePullRequestId(id: string): number | null {
+  const match = id.match(/^pr:(\d+)$/);
+  return match ? Number(match[1]) : null;
+}
+
+function pullRequestPatchPath(repo: string, number: number) {
+  const gitPath = runGit(repo, ["rev-parse", "--git-path", `hunk/pr-${number}.patch`]);
+  return isAbsolute(gitPath) ? gitPath : resolve(repo, gitPath);
+}
+
+function pullRequestLabelFromPath(path: string) {
+  const match = basename(path).match(/^pr-(\d+)\.patch$/);
+  return match ? `PR #${match[1]}` : null;
+}
+
+function isBuiltInReviewSourceId(id: string): id is BuiltInReviewSourceId {
+  return (
+    id === "unstaged" ||
+    id === "staged" ||
+    id === "commit" ||
+    id === "branch" ||
+    id === LAST_AGENT_TURN_SOURCE.id
+  );
+}
+
+function isReviewSourceId(id: string): id is ReviewSourceId {
+  return isBuiltInReviewSourceId(id) || parsePullRequestId(id) !== null;
+}
+
+function sourceMetadataPath(patchFile: string) {
+  return `${patchFile}.source.json`;
+}
+
+function sourceMetadataForPatch(patchFile: string | undefined) {
+  if (!patchFile) return null;
+
+  const metadataFile = sourceMetadataPath(patchFile);
+
+  if (!existsSync(metadataFile)) return null;
+
+  try {
+    const metadata: unknown = JSON.parse(readFileSync(metadataFile, "utf8"));
+    return metadata && typeof metadata === "object" ? (metadata as Record<string, unknown>) : null;
+  } catch {
+    return null;
+  }
+}
+
+function persistSourceMetadata(patchFile: string, metadata: Record<string, string>) {
+  try {
+    writeFileSync(sourceMetadataPath(patchFile), JSON.stringify(metadata));
+  } catch {
+    // Sidecar metadata should never prevent reviewing a readable patch.
+  }
+}
+
+function repoPathFromGitHunkPath(patchFile: string) {
+  const marker = `${sep}.git${sep}hunk${sep}`;
+  const markerIndex = patchFile.indexOf(marker);
+  return markerIndex === -1 ? undefined : patchFile.slice(0, markerIndex);
+}
+
+function currentBranch(repo: string) {
+  try {
+    return runGit(repo, ["branch", "--show-current"]);
+  } catch {
+    return "";
+  }
+}
+
+function openPullRequestsForBranch(repo: string): BranchPullRequest[] {
+  const branch = currentBranch(repo);
+  if (!branch) return [];
+
+  try {
+    const output = runGh(
+      repo,
+      ["pr", "list", "--state", "open", "--head", branch, "--json", "number,title"],
+      2_000,
+    );
+    const parsed: unknown = JSON.parse(output || "[]");
+    if (!Array.isArray(parsed)) return [];
+
+    return parsed.flatMap((item) => {
+      if (!item || typeof item !== "object") return [];
+      const record = item as Record<string, unknown>;
+      return typeof record.number === "number" && typeof record.title === "string"
+        ? [{ number: record.number, title: record.title }]
+        : [];
+    });
+  } catch {
+    return [];
+  }
+}
+
+function pullRequestSourceItems(repo: string | undefined, activeLabel: string | null) {
+  if (!repo) return [];
+
+  return openPullRequestsForBranch(repo).map((pr, index): ReviewSourceItem => {
+    const label = `PR #${pr.number} ${pr.title}`;
+    return {
+      id: `pr:${pr.number}`,
+      label,
+      hint: String(index + 1),
+      active: activeLabel === `PR #${pr.number}`,
+      enabled: true,
+    };
+  });
+}
+
+export function reviewSourceLabel(id: ReviewSourceId) {
+  const prNumber = parsePullRequestId(id);
+  if (prNumber !== null) return `PR #${prNumber}`;
+  if (isBuiltInReviewSourceId(id)) return sourceLabel(id);
+  return id;
+}
+
+export function activeReviewSource(input: CliInput): ReviewSourceId | null {
+  if (input.kind === "show") return "commit";
+  if (input.kind === "patch" && input.file) {
+    const metadata = sourceMetadataForPatch(input.file);
+    if (typeof metadata?.sourceId === "string" && isReviewSourceId(metadata.sourceId)) {
+      return metadata.sourceId;
+    }
+    const lastTurn = agentLastTurnSourceByPatch(input.file);
+    if (lastTurn) return LAST_AGENT_TURN_SOURCE.id;
+
+    const label = pullRequestLabelFromPath(input.file);
+    return label ? (`pr:${label.slice(4)}` as PullRequestReviewSourceId) : null;
+  }
+  if (input.kind !== "vcs") return null;
+  if (input.staged) return "staged";
+  if (input.range) return "branch";
+  return "unstaged";
+}
+
+export function sourceIndicatorLabel(input: CliInput, sourceLabel: string): string | null {
+  if (input.kind === "patch") {
+    const patchFile = input.file ?? sourceLabel;
+    const metadata = sourceMetadataForPatch(patchFile);
+    if (typeof metadata?.label === "string") return metadata.label;
+    return agentLastTurnSourceByPatch(patchFile)?.label ?? pullRequestLabelFromPath(patchFile);
+  }
+
+  const active = activeReviewSource(input);
+  return active ? reviewSourceLabel(active) : null;
+}
+
+export function buildReviewSourceItems(input: CliInput, repo?: string): ReviewSourceItem[] {
+  const active = activeReviewSource(input);
+  const activeLabel = sourceIndicatorLabel(input, "");
+  return [
+    ...REVIEW_SOURCE_ORDER.map((id) => ({
+      id,
+      label: sourceLabel(id),
+      hint: sourceHint(id),
+      active: active === id,
+      enabled: true,
+    })),
+    ...pullRequestSourceItems(repo, activeLabel),
+  ];
+}
+
+export function repoPathForInput(input: CliInput, sourceLabel: string): string | undefined {
+  if (input.kind === "vcs" || input.kind === "show" || input.kind === "stash-show") {
+    return sourceLabel;
+  }
+  if (input.kind !== "patch") return undefined;
+
+  const patchFile = input.file ?? sourceLabel;
+  const metadata = sourceMetadataForPatch(patchFile);
+  if (typeof metadata?.repo === "string") return metadata.repo;
+  return repoPathFromGitHunkPath(patchFile);
+}
+
+function defaultBranchRange(repo: string): string | null {
+  let baseRef = "";
+  try {
+    baseRef = runGit(repo, ["rev-parse", "--abbrev-ref", "--symbolic-full-name", "@{upstream}"]);
+  } catch {
+    for (const candidate of ["origin/main", "origin/master", "main", "master"]) {
+      try {
+        runGit(repo, ["rev-parse", "--verify", candidate]);
+        baseRef = candidate;
+        break;
+      } catch {
+        // Try next fallback.
+      }
+    }
+  }
+
+  if (!baseRef) return null;
+
+  try {
+    const mergeBase = runGit(repo, ["merge-base", "HEAD", baseRef]);
+    return mergeBase ? `${mergeBase}...HEAD` : "HEAD";
+  } catch {
+    return "HEAD";
+  }
+}
+
+function lastTurnMarker(repo: string, source: AgentLastTurnSource) {
+  const markerName = `hunk/${source.markerBasename}.json`;
+  try {
+    const marker = runGit(repo, ["rev-parse", "--git-path", markerName]);
+    return isAbsolute(marker) ? marker : resolve(repo, marker);
+  } catch {
+    return join(repo, ".git", markerName);
+  }
+}
+
+function newestLastAgentTurnMarker(repo: string) {
+  return AGENT_LAST_TURN_SOURCES.map((source) => ({
+    source,
+    markerPath: lastTurnMarker(repo, source),
+  }))
+    .filter(({ markerPath }) => existsSync(markerPath))
+    .sort((left, right) => statSync(right.markerPath).mtimeMs - statSync(left.markerPath).mtimeMs)
+    .at(0);
+}
+
+function resolveMarkerPath(repo: string, value: unknown) {
+  if (typeof value !== "string" || value.trim() === "") return null;
+  return isAbsolute(value) ? value : resolve(repo, value);
+}
+
+function lastAgentTurnInput(repo: string, options: CommonOptions): ReviewSourceSwitchResult {
+  const markerSource = newestLastAgentTurnMarker(repo);
+  if (!markerSource) {
+    return { kind: "notice", message: "No Last agent turn source for this repo." };
+  }
+
+  const { source, markerPath } = markerSource;
+
+  let marker: unknown;
+  try {
+    marker = JSON.parse(readFileSync(markerPath, "utf8"));
+  } catch {
+    return { kind: "notice", message: `${source.label} marker is unreadable.` };
+  }
+
+  if (!marker || typeof marker !== "object") {
+    return { kind: "notice", message: `${source.label} marker is invalid.` };
+  }
+
+  const record = marker as Record<string, unknown>;
+  if (record.kind === "patch") {
+    const file = resolveMarkerPath(repo, record.file);
+    if (!file || !existsSync(file)) {
+      return { kind: "notice", message: `${source.label} patch is missing.` };
+    }
+    persistSourceMetadata(file, {
+      repo,
+      label: source.label,
+      sourceId: LAST_AGENT_TURN_SOURCE.id,
+    });
+    return {
+      kind: "input",
+      input: { kind: "patch", file, options },
+      sourcePath: repo,
+      label: source.label,
+    };
+  }
+
+  return { kind: "notice", message: `${source.label} marker has no supported source.` };
+}
+
+function pullRequestInput(
+  repo: string,
+  number: number,
+  options: CommonOptions,
+): ReviewSourceSwitchResult {
+  let patchText = "";
+  try {
+    patchText = runGh(repo, ["pr", "diff", String(number), "--patch"]);
+  } catch {
+    return { kind: "notice", message: `Could not load PR #${number}.` };
+  }
+
+  if (!patchText) {
+    return { kind: "notice", message: `PR #${number} has no diff.` };
+  }
+
+  const file = pullRequestPatchPath(repo, number);
+  mkdirSync(dirname(file), { recursive: true });
+  writeFileSync(file, `${patchText}\n`);
+  persistSourceMetadata(file, { repo, label: `PR #${number}`, sourceId: `pr:${number}` });
+  return {
+    kind: "input",
+    input: { kind: "patch", file, options },
+    sourcePath: repo,
+    label: `PR #${number}`,
+  };
+}
+
+export function resolveReviewSourceInput(
+  sourceId: ReviewSourceId,
+  repo: string | undefined,
+  options: CommonOptions,
+): ReviewSourceSwitchResult {
+  if (!repo) {
+    return { kind: "notice", message: "Source switching requires a Git-backed Hunk session." };
+  }
+
+  const prNumber = parsePullRequestId(sourceId);
+  if (prNumber !== null) return pullRequestInput(repo, prNumber, options);
+
+  if (sourceId === LAST_AGENT_TURN_SOURCE.id) return lastAgentTurnInput(repo, options);
+
+  switch (sourceId) {
+    case "unstaged":
+      return {
+        kind: "input",
+        input: { kind: "vcs", staged: false, options },
+        sourcePath: repo,
+        label: BASE_SOURCE_LABELS.unstaged,
+      };
+    case "staged":
+      return {
+        kind: "input",
+        input: { kind: "vcs", staged: true, options },
+        sourcePath: repo,
+        label: BASE_SOURCE_LABELS.staged,
+      };
+    case "commit":
+      return {
+        kind: "input",
+        input: { kind: "show", ref: "HEAD", options },
+        sourcePath: repo,
+        label: BASE_SOURCE_LABELS.commit,
+      };
+    case "branch": {
+      const range = defaultBranchRange(repo);
+      if (!range) {
+        return { kind: "notice", message: "No upstream or default branch found." };
+      }
+      return {
+        kind: "input",
+        input: { kind: "vcs", range, staged: false, options },
+        sourcePath: repo,
+        label: BASE_SOURCE_LABELS.branch,
+      };
+    }
+  }
+
+  return { kind: "notice", message: "Unsupported source." };
+}
diff --git a/test/pty/chrome.test.ts b/test/pty/chrome.test.ts
index 0eea9d9..73a7b79 100644
--- a/test/pty/chrome.test.ts
+++ b/test/pty/chrome.test.ts
@@ -174,7 +174,7 @@ describe("PTY chrome", () => {
     });
 
     try {
-      const initial = await session.waitForText(/View\s+Navigate\s+Agent\s+Help/, {
+      const initial = await session.waitForText(/Source\s+View\s+Navigate\s+Agent\s+Help/, {
         timeout: 15_000,
       });
 
@@ -209,7 +209,7 @@ describe("PTY chrome", () => {
     });
 
     try {
-      const initial = await session.waitForText(/View\s+Navigate\s+Agent\s+Help/, {
+      const initial = await session.waitForText(/Source\s+View\s+Navigate\s+Agent\s+Help/, {
         timeout: 15_000,
       });
 
@@ -252,7 +252,7 @@ describe("PTY chrome", () => {
     });
 
     try {
-      await session.waitForText(/View\s+Navigate\s+Agent\s+Help/, {
+      await session.waitForText(/Source\s+View\s+Navigate\s+Agent\s+Help/, {
         timeout: 15_000,
       });
 
@@ -282,7 +282,7 @@ describe("PTY chrome", () => {
     });
 
     try {
-      const initial = await session.waitForText(/View\s+Navigate\s+Agent\s+Help/, {
+      const initial = await session.waitForText(/Source\s+View\s+Navigate\s+Agent\s+Help/, {
         timeout: 15_000,
       });
 
@@ -323,7 +323,7 @@ describe("PTY chrome", () => {
     });
 
     try {
-      const initial = await session.waitForText(/View\s+Navigate\s+Agent\s+Help/, {
+      const initial = await session.waitForText(/Source\s+View\s+Navigate\s+Agent\s+Help/, {
         timeout: 15_000,
       });
 
diff --git a/test/pty/pager.test.ts b/test/pty/pager.test.ts
index dab2b0d..f7efa2b 100644
--- a/test/pty/pager.test.ts
+++ b/test/pty/pager.test.ts
@@ -48,7 +48,7 @@ describe("PTY pager", () => {
     try {
       const initial = await session.waitForText(/scroll\.ts/, { timeout: 15_000 });
 
-      expect(initial).not.toContain("View  Navigate  Agent  Help");
+      expect(initial).not.toContain("Source  View  Navigate  Agent  Help");
       expect(initial).toContain("before_01");
       expect(initial).not.toContain("before_23");
 
@@ -61,7 +61,7 @@ describe("PTY pager", () => {
         5_000,
       );
 
-      expect(paged).not.toContain("View  Navigate  Agent  Help");
+      expect(paged).not.toContain("Source  View  Navigate  Agent  Help");
       expect(paged).toContain("before_23");
     } finally {
       session.close();
@@ -152,7 +152,7 @@ describe("PTY pager", () => {
     });
 
     try {
-      const initial = await session.waitForText(/View\s+Navigate\s+Agent\s+Help/, {
+      const initial = await session.waitForText(/Source\s+View\s+Navigate\s+Agent\s+Help/, {
         timeout: 15_000,
       });
       expect(initial).toContain("alpha.ts");
@@ -176,7 +176,7 @@ describe("PTY pager", () => {
     try {
       const initial = await session.waitForText(/scroll\.ts/, { timeout: 15_000 });
 
-      expect(initial).not.toContain("View  Navigate  Agent  Help");
+      expect(initial).not.toContain("Source  View  Navigate  Agent  Help");
       expect(initial).toContain("before_01");
       expect(initial).not.toContain("before_12");
 
@@ -187,7 +187,7 @@ describe("PTY pager", () => {
         (text) => !text.includes("before_01") && text.includes("before_12"),
       );
 
-      expect(scrolled).not.toContain("View  Navigate  Agent  Help");
+      expect(scrolled).not.toContain("Source  View  Navigate  Agent  Help");
       expect(scrolled).not.toContain("before_01");
       expect(scrolled).toContain("before_12");
 
@@ -244,7 +244,7 @@ describe("PTY pager", () => {
     try {
       const initial = await session.waitForText(/scroll\.ts/, { timeout: 15_000 });
 
-      expect(initial).not.toContain("View  Navigate  Agent  Help");
+      expect(initial).not.toContain("Source  View  Navigate  Agent  Help");
       expect(initial).toContain("before_01");
       expect(initial).not.toContain("before_12");
 
@@ -255,7 +255,7 @@ describe("PTY pager", () => {
         (text) => !text.includes("before_01") && text.includes("before_12"),
       );
 
-      expect(scrolled).not.toContain("View  Navigate  Agent  Help");
+      expect(scrolled).not.toContain("Source  View  Navigate  Agent  Help");
       expect(scrolled).not.toContain("before_01");
       expect(scrolled).toContain("before_12");
 
@@ -284,7 +284,7 @@ describe("PTY pager", () => {
     try {
       const initial = await session.waitForText(/scroll\.ts/, { timeout: 15_000 });
 
-      expect(initial).not.toContain("View  Navigate  Agent  Help");
+      expect(initial).not.toContain("Source  View  Navigate  Agent  Help");
       expect(harness.countMatches(initial, /scroll\.ts/g)).toBe(1);
 
       // CI can surface the pager content before the file-backed stdin path is ready for keys.
@@ -297,7 +297,7 @@ describe("PTY pager", () => {
         5_000,
       );
 
-      expect(withSidebar).not.toContain("View  Navigate  Agent  Help");
+      expect(withSidebar).not.toContain("Source  View  Navigate  Agent  Help");
       expect(withSidebar).toMatch(sidebarRow);
     } finally {
       session.close();
@@ -436,7 +436,7 @@ describe("PTY pager", () => {
     try {
       const initial = await session.waitForText(/scroll\.ts/, { timeout: 15_000 });
 
-      expect(initial).not.toContain("View  Navigate  Agent  Help");
+      expect(initial).not.toContain("Source  View  Navigate  Agent  Help");
       expect(initial).toContain("before_01");
       expect(initial).not.toContain("before_12");
 
@@ -447,7 +447,7 @@ describe("PTY pager", () => {
         (text) => !text.includes("before_01") && text.includes("before_12"),
       );
 
-      expect(scrolled).not.toContain("View  Navigate  Agent  Help");
+      expect(scrolled).not.toContain("Source  View  Navigate  Agent  Help");
       expect(scrolled).not.toContain("before_01");
       expect(scrolled).toContain("before_12");
 
