diff --git a/packages/hunk/src/app/cli.test.ts b/packages/hunk/src/app/cli.test.ts
index 856bf88d..524d0063 100644
--- a/packages/hunk/src/app/cli.test.ts
+++ b/packages/hunk/src/app/cli.test.ts
@@ -10,6 +10,7 @@ import {
   WATCH_OPTION,
 } from "./cli";
 import { resolveCliVersion } from "../core/run/version";
+import { persistResumableReview } from "../core/resumableReview";
 
 const tempDirs: string[] = [];
 
@@ -19,6 +20,21 @@ function createTempDir(prefix: string) {
   return dir;
 }
 
+function git(cwd: string, ...args: string[]) {
+  const result = Bun.spawnSync(["git", ...args], { cwd, stderr: "pipe", stdout: "pipe" });
+  if (result.exitCode !== 0) throw new Error(result.stderr.toString());
+}
+
+async function withCwd<T>(cwd: string, fn: () => Promise<T>) {
+  const previous = process.cwd();
+  process.chdir(cwd);
+  try {
+    return await fn();
+  } finally {
+    process.chdir(previous);
+  }
+}
+
 /** Run `fn` with Bun.stdin.stream replaced by a one-shot reader of `text`. */
 async function withStdin<T>(text: string, fn: () => Promise<T>): Promise<T> {
   const originalStdin = Bun.stdin.stream;
@@ -82,6 +98,53 @@ describe("parseCli", () => {
     expect(parsed.text).not.toContain("Config:");
     expect(parsed.text).not.toContain("Examples:");
   });
+  test("lists and parses resumable review mode", async () => {
+    const repo = createTempDir("hunk-cli-resume-");
+    git(repo, "init");
+    persistResumableReview(
+      {
+        kind: "vcs",
+        range: "HEAD~1",
+        staged: true,
+        pathspecs: ["src"],
+        options: { mode: "unified" },
+      },
+      repo,
+    );
+
+    const parsed = await withCwd(repo, () => parseCli(["bun", "hunk", "resume"]));
+    expect(parsed).toEqual({
+      kind: "vcs",
+      range: "HEAD~1",
+      staged: true,
+      pathspecs: ["src"],
+      options: { mode: "unified" },
+    });
+
+    const help = await parseCli(["bun", "hunk"]);
+    expect(help.kind === "help" ? help.text : "").toContain("hunk resume");
+    const resumeHelp = await parseCli(["bun", "hunk", "resume", "--help"]);
+    expect(resumeHelp.kind === "help" ? resumeHelp.text : "").toContain("Reopen");
+  });
+
+  test("resume warns once and safely falls back when no valid marker exists", async () => {
+    const repo = createTempDir("hunk-cli-resume-missing-");
+    git(repo, "init");
+    const messages: string[] = [];
+    const originalWrite = process.stderr.write;
+    process.stderr.write = ((message: string) => {
+      messages.push(message);
+      return true;
+    }) as typeof process.stderr.write;
+    try {
+      const parsed = await withCwd(repo, () => parseCli(["bun", "hunk", "resume"]));
+      expect(parsed).toEqual({ kind: "vcs", staged: false, options: {} });
+      expect(messages).toHaveLength(1);
+      expect(messages[0]).toContain("no resumable review");
+    } finally {
+      process.stderr.write = originalWrite;
+    }
+  });
 
   test("prints the same top-level help for --help", async () => {
     const bare = await parseCli(["bun", "hunk"]);
diff --git a/packages/hunk/src/app/cli.ts b/packages/hunk/src/app/cli.ts
index f5ed164a..b34164c4 100644
--- a/packages/hunk/src/app/cli.ts
+++ b/packages/hunk/src/app/cli.ts
@@ -57,6 +57,7 @@ import {
 import { DEFAULT_FILE_GAP, DEFAULT_HUNK_GAP, parseReviewGap } from "../core/run/reviewGap";
 import { DEFAULT_TAB_WIDTH, parseTabWidth } from "../core/run/tabWidth";
 import { resolveCliVersion } from "../core/run/version";
+import { loadResumableReview } from "../core/resumableReview";
 import type { ExtensionVcsHistoryReviewAction } from "../extension-api/types";
 
 /** Structured option metadata shared by Commander registration and generated CLI docs. */
@@ -566,6 +567,26 @@ function renderSkillHelp() {
   ].join("\n");
 }
 
+/** Resolve `hunk resume` to the worktree's last reloadable review input. */
+function parseResumeCommand(tokens: string[]): CliInput | HelpCommandInput {
+  if (tokens.length === 1 && (tokens[0] === "--help" || tokens[0] === "-h")) {
+    return {
+      kind: "help",
+      text: "Usage: hunk resume\n\nReopen the last reloadable review source for this Git worktree.\n",
+    };
+  }
+  if (tokens.length > 0) {
+    throw new Error("`hunk resume` does not accept additional arguments.");
+  }
+
+  const input = loadResumableReview(process.cwd());
+  if (input) return input;
+  process.stderr.write(
+    "Warning: no resumable review found for this worktree; opening working tree changes.\n",
+  );
+  return { kind: "vcs", staged: false, options: {} };
+}
+
 /** Build the top-level help text shown by bare `hunk` and `hunk --help`. */
 function renderCliHelp() {
   return [
@@ -582,6 +603,7 @@ function renderCliHelp() {
     "  hunk log [target] [-- <pathspec...>]    browse an attractive repository history",
     "  hunk stash show [ref]                   review a stash entry (git only)",
     "  hunk patch [file]                       review a patch file or stdin",
+    "  hunk resume                             reopen this worktree's last review source",
     "  hunk pager                              general Git pager wrapper with diff detection",
     "  hunk difftool <left> <right> [path]     review Git difftool file pairs",
     "  hunk session <subcommand>               inspect or control a live Hunk session",
@@ -2333,6 +2355,8 @@ export async function parseCli(argv: string[]): Promise<ParsedCliInput> {
   // Host bootstrap options must stay before a review command's `--` pathspec separator.
   const reviewRest = [...extensionFlagTokens, ...rest];
   switch (commandName) {
+    case "resume":
+      return parseResumeCommand(rest);
     case "diff":
       return parseDiffCommand(reviewRest, argv);
     case "show":
diff --git a/packages/hunk/src/core/resumableReview.test.ts b/packages/hunk/src/core/resumableReview.test.ts
new file mode 100644
index 00000000..52482623
--- /dev/null
+++ b/packages/hunk/src/core/resumableReview.test.ts
@@ -0,0 +1,190 @@
+import { existsSync, mkdtempSync, mkdirSync, readFileSync, rmSync, writeFileSync } from "node:fs";
+import { tmpdir } from "node:os";
+import { dirname, join } from "node:path";
+import { afterEach, describe, expect, test } from "bun:test";
+import type { CliInput } from "./run/commandInputs";
+import { loadResumableReview, persistResumableReview } from "./resumableReview";
+
+const tempDirs: string[] = [];
+
+function tempDir(name: string) {
+  const dir = mkdtempSync(join(tmpdir(), name));
+  tempDirs.push(dir);
+  return dir;
+}
+
+function git(cwd: string, ...args: string[]) {
+  const result = Bun.spawnSync(["git", ...args], {
+    cwd,
+    stderr: "pipe",
+    stdout: "pipe",
+  });
+  if (result.exitCode !== 0) throw new Error(result.stderr.toString());
+  return result.stdout.toString().trim();
+}
+
+function createRepo() {
+  const repo = tempDir("hunk-resume-");
+  git(repo, "init");
+  git(repo, "config", "user.email", "test@example.com");
+  git(repo, "config", "user.name", "Test");
+  writeFileSync(join(repo, "tracked.txt"), "base\n");
+  git(repo, "add", "tracked.txt");
+  git(repo, "commit", "-m", "base");
+  return repo;
+}
+
+function markerPath(repo: string) {
+  return join(git(repo, "rev-parse", "--absolute-git-dir"), "hunk", "resume.json");
+}
+
+afterEach(() => {
+  for (const dir of tempDirs.splice(0)) rmSync(dir, { force: true, recursive: true });
+});
+
+describe("resumable review markers", () => {
+  test("persists and reloads every reopenable input shape without dropping source options", () => {
+    const repo = createRepo();
+    const inputs: CliInput[] = [
+      {
+        kind: "vcs",
+        range: "HEAD~1..HEAD",
+        staged: true,
+        pathspecs: ["src"],
+        options: {
+          agentContext: "agent.json",
+          agentNotes: true,
+          colorMoved: false,
+          copyDecorations: true,
+          cursorLine: "number",
+          excludeUntracked: true,
+          experimental: true,
+          extensionPaths: ["extension.ts"],
+          extensions: false,
+          fast: true,
+          fileGap: 2,
+          hunkGap: 3,
+          hunkHeaders: false,
+          lineNumbers: false,
+          menuBar: false,
+          mode: "split",
+          pager: true,
+          promptSaveViewPreferences: false,
+          sidebar: "auto",
+          tabWidth: 8,
+          theme: "dark",
+          transparentBackground: true,
+          vcs: "git",
+          watch: true,
+          wrapLines: true,
+        },
+      },
+      {
+        kind: "show",
+        ref: "HEAD",
+        pathspecs: ["tracked.txt"],
+        options: { theme: "dark" },
+      },
+      { kind: "stash-show", ref: "stash@{1}", options: { lineNumbers: false } },
+      {
+        kind: "diff",
+        left: "before",
+        right: "after",
+        options: { wrapLines: true },
+      },
+      { kind: "patch", file: "changes.patch", options: { pager: true } },
+      {
+        kind: "difftool",
+        left: "old",
+        right: "new",
+        path: "tracked.txt",
+        options: { colorMoved: false },
+      },
+    ];
+
+    for (const input of inputs) {
+      persistResumableReview(input, repo);
+      expect(loadResumableReview(repo)).toEqual(input);
+      const marker = JSON.parse(readFileSync(markerPath(repo), "utf8"));
+      expect(marker).toMatchObject({ version: 1, input });
+      expect(new Date(marker.updatedAt).toISOString()).toBe(marker.updatedAt);
+    }
+  });
+
+  test("reopens a 0.21 stacked review with the renamed unified layout", () => {
+    const repo = createRepo();
+    const marker = markerPath(repo);
+    mkdirSync(dirname(marker), { recursive: true });
+    writeFileSync(
+      marker,
+      JSON.stringify({
+        version: 1,
+        updatedAt: "2026-09-01T00:00:00.000Z",
+        input: { kind: "show", ref: "HEAD~1", options: { mode: "stack" } },
+      }),
+    );
+
+    expect(loadResumableReview(repo)).toEqual({
+      kind: "show",
+      ref: "HEAD~1",
+      options: { mode: "unified" },
+    });
+  });
+
+  test("stores distinct markers for linked worktrees", () => {
+    const repo = createRepo();
+    const linked = tempDir("hunk-resume-linked-");
+    rmSync(linked, { recursive: true });
+    git(repo, "worktree", "add", "-b", "linked", linked);
+
+    persistResumableReview({ kind: "vcs", staged: false, options: {} }, repo);
+    persistResumableReview({ kind: "show", ref: "HEAD", options: {} }, linked);
+
+    expect(markerPath(linked)).not.toBe(markerPath(repo));
+    expect(loadResumableReview(repo)?.kind).toBe("vcs");
+    expect(loadResumableReview(linked)?.kind).toBe("show");
+  });
+
+  test("returns null for missing, malformed, unsupported, or invalid markers", () => {
+    const repo = createRepo();
+    expect(loadResumableReview(repo)).toBeNull();
+
+    const marker = markerPath(repo);
+    mkdirSync(dirname(marker), { recursive: true });
+    for (const contents of [
+      "not json",
+      JSON.stringify({
+        version: 2,
+        updatedAt: new Date().toISOString(),
+        input: { kind: "vcs", staged: false, options: {} },
+      }),
+      JSON.stringify({
+        version: 1,
+        updatedAt: "invalid",
+        input: { kind: "vcs", staged: "no", options: {} },
+      }),
+      JSON.stringify({
+        version: 1,
+        updatedAt: new Date().toISOString(),
+        input: { kind: "patch", text: "diff", options: {} },
+      }),
+    ]) {
+      writeFileSync(marker, contents);
+      expect(loadResumableReview(repo)).toBeNull();
+    }
+  });
+
+  test("stdin-backed inputs remove an existing marker", () => {
+    const repo = createRepo();
+    for (const input of [
+      { kind: "patch", text: "diff --git a/a b/a\n", options: {} },
+      { kind: "vcs", staged: false, options: { agentContext: "-" } },
+    ] satisfies CliInput[]) {
+      persistResumableReview({ kind: "vcs", staged: true, options: {} }, repo);
+      expect(existsSync(markerPath(repo))).toBe(true);
+      persistResumableReview(input, repo);
+      expect(existsSync(markerPath(repo))).toBe(false);
+      expect(loadResumableReview(repo)).toBeNull();
+    }
+  });
+});
diff --git a/packages/hunk/src/core/resumableReview.ts b/packages/hunk/src/core/resumableReview.ts
new file mode 100644
index 00000000..8471bd81
--- /dev/null
+++ b/packages/hunk/src/core/resumableReview.ts
@@ -0,0 +1,216 @@
+import { mkdirSync, readFileSync, renameSync, rmSync, writeFileSync } from "node:fs";
+import { dirname, join } from "node:path";
+import type { CliInput, CommonOptions } from "./run/commandInputs";
+import { canReloadInput } from "./run/inputReload";
+
+const MARKER_VERSION = 1;
+
+function isRecord(value: unknown): value is Record<string, unknown> {
+  return typeof value === "object" && value !== null && !Array.isArray(value);
+}
+
+function readCommonOptions(value: unknown): CommonOptions | null {
+  if (!isRecord(value)) return null;
+
+  const options: CommonOptions = {};
+  if (value.mode !== undefined) {
+    // Version 1 markers written before 0.22 used "stack" for this layout.
+    const mode = value.mode === "stack" ? "unified" : value.mode;
+    if (mode !== "auto" && mode !== "split" && mode !== "unified") return null;
+    options.mode = mode;
+  }
+  if (value.cursorLine !== undefined) {
+    if (value.cursorLine !== "row" && value.cursorLine !== "number" && value.cursorLine !== "off") {
+      return null;
+    }
+    options.cursorLine = value.cursorLine;
+  }
+  if (value.sidebar !== undefined) {
+    if (typeof value.sidebar !== "boolean" && value.sidebar !== "auto") return null;
+    options.sidebar = value.sidebar;
+  }
+  for (const key of ["vcs", "theme", "agentContext"] as const) {
+    const option = value[key];
+    if (option !== undefined) {
+      if (typeof option !== "string") return null;
+      options[key] = option;
+    }
+  }
+  for (const key of [
+    "pager",
+    "menuBar",
+    "watch",
+    "experimental",
+    "fast",
+    "excludeUntracked",
+    "lineNumbers",
+    "wrapLines",
+    "hunkHeaders",
+    "agentNotes",
+    "copyDecorations",
+    "promptSaveViewPreferences",
+    "transparentBackground",
+    "colorMoved",
+    "extensions",
+  ] as const) {
+    const option = value[key];
+    if (option !== undefined) {
+      if (typeof option !== "boolean") return null;
+      options[key] = option;
+    }
+  }
+  for (const [key, min, max] of [
+    ["tabWidth", 1, 16],
+    ["fileGap", 0, 8],
+    ["hunkGap", 0, 8],
+  ] as const) {
+    const option = value[key];
+    if (option !== undefined) {
+      if (typeof option !== "number" || !Number.isInteger(option) || option < min || option > max) {
+        return null;
+      }
+      options[key] = option;
+    }
+  }
+  const extensionPaths = readOptionalStrings(value.extensionPaths);
+  if (extensionPaths === null) return null;
+  if (extensionPaths !== undefined) options.extensionPaths = extensionPaths;
+  return options;
+}
+
+function readOptionalString(value: unknown) {
+  return value === undefined || typeof value === "string" ? value : null;
+}
+
+function readOptionalStrings(value: unknown) {
+  if (value === undefined) return undefined;
+  return Array.isArray(value) && value.every((item) => typeof item === "string") ? value : null;
+}
+
+function readCliInput(value: unknown): CliInput | null {
+  if (!isRecord(value) || typeof value.kind !== "string") return null;
+  const options = readCommonOptions(value.options);
+  if (!options) return null;
+
+  switch (value.kind) {
+    case "vcs": {
+      const range = readOptionalString(value.range);
+      const pathspecs = readOptionalStrings(value.pathspecs);
+      if (typeof value.staged !== "boolean" || range === null || pathspecs === null) return null;
+      return {
+        kind: "vcs",
+        ...(range === undefined ? {} : { range }),
+        staged: value.staged,
+        ...(pathspecs === undefined ? {} : { pathspecs }),
+        options,
+      };
+    }
+    case "show": {
+      const ref = readOptionalString(value.ref);
+      const pathspecs = readOptionalStrings(value.pathspecs);
+      if (ref === null || pathspecs === null) return null;
+      return {
+        kind: "show",
+        ...(ref === undefined ? {} : { ref }),
+        ...(pathspecs === undefined ? {} : { pathspecs }),
+        options,
+      };
+    }
+    case "stash-show": {
+      const ref = readOptionalString(value.ref);
+      if (ref === null) return null;
+      return {
+        kind: "stash-show",
+        ...(ref === undefined ? {} : { ref }),
+        options,
+      };
+    }
+    case "diff":
+      return typeof value.left === "string" && typeof value.right === "string"
+        ? { kind: "diff", left: value.left, right: value.right, options }
+        : null;
+    case "patch":
+      return typeof value.file === "string" && value.text === undefined
+        ? { kind: "patch", file: value.file, options }
+        : null;
+    case "difftool": {
+      const path = readOptionalString(value.path);
+      if (typeof value.left !== "string" || typeof value.right !== "string" || path === null) {
+        return null;
+      }
+      return {
+        kind: "difftool",
+        left: value.left,
+        right: value.right,
+        ...(path === undefined ? {} : { path }),
+        options,
+      };
+    }
+    default:
+      return null;
+  }
+}
+
+function markerPath(cwd: string) {
+  const result = Bun.spawnSync(["git", "rev-parse", "--absolute-git-dir"], {
+    cwd,
+    stdin: "ignore",
+    stdout: "pipe",
+    stderr: "pipe",
+  });
+  if (result.exitCode !== 0)
+    throw new Error(result.stderr.toString().trim() || "not a Git worktree");
+  return join(result.stdout.toString().trim(), "hunk", "resume.json");
+}
+
+function warn(message: string) {
+  process.stderr.write(`Warning: ${message}\n`);
+}
+
+/** Persist the latest reloadable review source for one Git worktree. */
+export function persistResumableReview(input: CliInput, cwd = process.cwd()): void {
+  let path: string;
+  try {
+    path = markerPath(cwd);
+    const normalized = canReloadInput(input) ? readCliInput(input) : null;
+    if (!normalized) {
+      rmSync(path, { force: true });
+      return;
+    }
+
+    mkdirSync(dirname(path), { recursive: true });
+    const temporaryPath = `${path}.${process.pid}.${crypto.randomUUID()}.tmp`;
+    try {
+      writeFileSync(
+        temporaryPath,
+        `${JSON.stringify({ version: MARKER_VERSION, updatedAt: new Date().toISOString(), input: normalized }, null, 2)}\n`,
+        { flag: "wx" },
+      );
+      renameSync(temporaryPath, path);
+    } finally {
+      rmSync(temporaryPath, { force: true });
+    }
+  } catch (error) {
+    warn(
+      `could not persist resumable review: ${error instanceof Error ? error.message : String(error)}`,
+    );
+  }
+}
+
+/** Load the latest validated reloadable review source for one Git worktree. */
+export function loadResumableReview(cwd = process.cwd()): CliInput | null {
+  try {
+    const marker: unknown = JSON.parse(readFileSync(markerPath(cwd), "utf8"));
+    if (!isRecord(marker) || marker.version !== MARKER_VERSION) return null;
+    if (
+      typeof marker.updatedAt !== "string" ||
+      new Date(marker.updatedAt).toISOString() !== marker.updatedAt
+    ) {
+      return null;
+    }
+    const input = readCliInput(marker.input);
+    return input && canReloadInput(input) ? input : null;
+  } catch {
+    return null;
+  }
+}
diff --git a/packages/hunk/src/main.tsx b/packages/hunk/src/main.tsx
index 80a6caff..1d6d3b84 100644
--- a/packages/hunk/src/main.tsx
+++ b/packages/hunk/src/main.tsx
@@ -4,6 +4,7 @@ import { formatCliError } from "./core/run/errors";
 import { pagePlainText } from "./core/process/pager";
 import { writeStdout } from "./core/process/stdout";
 import { prepareStartupPlan } from "./app/startup";
+import { persistResumableReview } from "./core/resumableReview";
 import { sanitizeTerminalLine, sanitizeTerminalText } from "./lib/terminalText";
 import { serveSessionBrokerDaemon } from "./session/broker/brokerServer";
 import { runSessionCommand } from "./session/agent/commands";
@@ -152,6 +153,7 @@ async function main() {
   if (startupPlan.kind !== "app") {
     throw new Error("Unreachable startup plan.");
   }
+  persistResumableReview(startupPlan.bootstrap.input);
 
   // OpenTUI stays behind the interactive plan so headless commands never materialize its embedded
   // native library. The shared interactive runner owns the highlighting worker and terminal until
diff --git a/packages/hunk/src/ui/AppHost.interactions.test.tsx b/packages/hunk/src/ui/AppHost.interactions.test.tsx
index 8091c72a..15513ece 100644
--- a/packages/hunk/src/ui/AppHost.interactions.test.tsx
+++ b/packages/hunk/src/ui/AppHost.interactions.test.tsx
@@ -14,6 +14,7 @@ import type {
 import { LEGACY_CUSTOM_SYNTAX_NOTICE } from "../core/process/startupNotice";
 import type { AppBootstrap } from "../core/bootstrap";
 import type { LayoutMode } from "../core/run/commandInputs";
+import { loadResumableReview } from "../core/resumableReview";
 import { createTestVcsAppBootstrap } from "../../../../test/helpers/app-bootstrap";
 import { capturedTestColorToHex } from "../../../../test/helpers/test-color-helpers";
 import {
@@ -1721,6 +1722,8 @@ describe("App interactions", () => {
   test("session reload preserves live comments while refreshing the file diff", async () => {
     const dir = mkdtempSync(join(process.cwd(), ".hunk-session-reload-"));
     const left = join(dir, "before.ts");
+    const git = Bun.spawnSync(["git", "init"], { cwd: dir, stderr: "pipe", stdout: "pipe" });
+    if (git.exitCode !== 0) throw new Error(git.stderr.toString());
     const right = join(dir, "after.ts");
     const reviewNote = "Keep this daemon review note";
 
@@ -1735,7 +1738,7 @@ describe("App interactions", () => {
         mode: "split",
       },
     });
-    const { dispatchCommand, hostClient } = createMockHostClient();
+    const { dispatchCommand, hostClient } = createMockHostClient({ cwd: dir, repoRoot: dir });
 
     const setup = await testRender(<AppHost bootstrap={bootstrap} hostClient={hostClient} />, {
       width: 220,
@@ -1793,6 +1796,14 @@ describe("App interactions", () => {
 
       expect(frame).toContain("export const added = true;");
       expect(frame).toContain(reviewNote);
+      expect(loadResumableReview(dir)).toMatchObject({
+        kind: "diff",
+        left,
+        right,
+        options: {
+          mode: "split",
+        },
+      });
     } finally {
       await act(async () => {
         setup.renderer.destroy();
diff --git a/packages/hunk/src/ui/AppHost.reload.test.tsx b/packages/hunk/src/ui/AppHost.reload.test.tsx
index 8616c3b2..bf1e3f71 100644
--- a/packages/hunk/src/ui/AppHost.reload.test.tsx
+++ b/packages/hunk/src/ui/AppHost.reload.test.tsx
@@ -580,7 +580,7 @@ describe("reload agent attention marks", () => {
     writeFileSync(file, "base\nstaged change\nunstaged change\n");
 
     const bootstrap = await loadAppBootstrap(
-      { kind: "vcs", staged: false, options: { mode: "stack", excludeUntracked: true } },
+      { kind: "vcs", staged: false, options: { mode: "unified", excludeUntracked: true } },
       { cwd: dir, vcsCatalog: getBundledVcsCatalog() },
     );
 
@@ -645,7 +645,7 @@ describe("reload agent attention marks", () => {
     writeFileSync(file, "base\nstaged change\nunstaged change\n");
 
     const bootstrap = await loadAppBootstrap(
-      { kind: "vcs", staged: false, options: { mode: "stack", excludeUntracked: true } },
+      { kind: "vcs", staged: false, options: { mode: "unified", excludeUntracked: true } },
       { cwd: dir, vcsCatalog: getBundledVcsCatalog() },
     );
 
diff --git a/packages/hunk/src/ui/AppHost.tsx b/packages/hunk/src/ui/AppHost.tsx
index 74265bb5..6f34670c 100644
--- a/packages/hunk/src/ui/AppHost.tsx
+++ b/packages/hunk/src/ui/AppHost.tsx
@@ -11,6 +11,7 @@ import { resolveRuntimeCliInput } from "../core/process/terminal";
 import type { StartupNotice } from "../core/process/startupNotice";
 import type { AppBootstrap } from "../core/bootstrap";
 import type { CliInput } from "../core/run/commandInputs";
+import { persistResumableReview } from "../core/resumableReview";
 import type { ExtensionReviewReloadResult } from "../extension-api/types";
 import type { ExtensionLoadResult } from "../extensions/types";
 import {
@@ -382,6 +383,7 @@ export function AppHost({
                 createUnknownVcsNotice(sessionVcs.unknownVcsId, String(reloadInput.options.vcs)),
               ]
             : configured.startupNotices;
+        persistResumableReview(nextBootstrap.input, hostClient?.getRegistration().cwd ?? cwd);
         const preparedPublication = producer.preparePublication({
           files: nextBootstrap.changeset.files,
           sourceLabel: nextBootstrap.changeset.sourceLabel,
