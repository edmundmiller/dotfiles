# Ghostty Configuration
# Main configuration file
#
# This configuration is split into multiple files for better organization.
# Files are loaded in order, with later values overriding earlier ones.

# Core configuration
config-file = ui.conf
config-file = behavior.conf
config-file = keybindings.conf

# Platform-specific configuration
config-file = macos.conf

# Optional local overrides (machine-specific settings)
# Create this file if you need per-machine customization
config-file = ?local.conf
