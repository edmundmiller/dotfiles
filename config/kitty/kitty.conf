# Kitty Configuration
# NOTE: Most settings are managed by Nix in modules/desktop/term/kitty.nix
# This file is kept for reference and non-Nix systems only.
#
# Visual settings synced from Ghostty config/ghostty/ui.conf:
# - Font: Maple Mono NF, size 14, ligatures enabled
# - Theme: Catppuccin Mocha (dark) / Latte (light)
# - Window padding: 8px
# - Colorspace: Display P3
# - Window decorations: enabled
# - Initial size: 1200x800
