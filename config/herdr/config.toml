[theme]
name = "terminal"

[theme.custom]
panel_bg = "reset"
surface0 = "#ccd0da"
surface1 = "#bcc0cc"
surface_dim = "#dce0e8"
overlay0 = "#8c8fa1"
overlay1 = "#7c7f93"
text = "#4c4f69"
subtext0 = "#6c6f85"
accent = "#1e66f5"
blue = "#1e66f5"
green = "#40a02b"
yellow = "#df8e1d"
red = "#d20f39"
teal = "#179299"
peach = "#fe640b"
mauve = "#8839ef"

[ui.toast]
delivery = "herdr"


[session]
resume_agents_on_restore = true

[experimental]
pane_history = true

[keys]
# Show upstream defaults with: herdr --default-config
# Upstream Herdr defaults use ctrl+b as the prefix, prefix+g for goto,
# and prefix+shift+g for new_worktree. This repo keeps ctrl+c as the
# tmux-style prefix and reserves prefix+g for the native worktree flow.
prefix = "ctrl+c"
new_workspace = "prefix+w"
new_worktree = "prefix+g"
goto = "prefix+f"
open_worktree = "prefix+G"
workspace_picker = "prefix+O"
split_horizontal = "prefix+-"
# Herdr has a hard-coded navigate-mode q quit/detach fallback.
# Bind q to a harmless configured action so prefix+q no longer quits.
toggle_sidebar = "prefix+b"

previous_tab = "prefix+p"
next_tab = "prefix+n"

[[keys.command]]
key = "prefix+u"
type = "plugin_action"
command = "dotfiles.dev-layout.hunk-split"
description = "open hunk diff in a side pane"

[[keys.command]]
key = "prefix+U"
type = "plugin_action"
command = "dotfiles.dev-layout.hunk-tab"
description = "open hunk diff in a tab"

[[keys.command]]
key = "prefix+a"
type = "plugin_action"
command = "nathanflurry.jj-workspace.new"
description = "new jj workspace"

[[keys.command]]
key = "prefix+A"
type = "plugin_action"
command = "nathanflurry.jj-workspace.new-tab"
description = "new jj workspace in a tab"

[[keys.command]]
key = "prefix+d"
type = "plugin_action"
command = "nathanflurry.jj-workspace.remove"
description = "remove jj workspace"

[worktrees]
directory = "~/.local/share/herdr/worktrees"
