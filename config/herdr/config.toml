[theme]
name = "catppuccin"

[ui.toast]
delivery = "herdr"

[keys]
prefix = "ctrl+c"
new_workspace = "prefix+w"
new_worktree = "prefix+g"
open_worktree = "prefix+G"
workspace_picker = "prefix+O"
split_horizontal = "prefix+-"
# Herdr has a hard-coded navigate-mode q quit/detach fallback.
# Bind q to a harmless configured action so prefix+q no longer quits.
toggle_sidebar = "prefix+b"

previous_tab = "prefix+p"
next_tab = "prefix+n"

[[keys.command]]
key = "prefix+["
type = "shell"
command = "herdr-hunk"

[[keys.command]]
key = "prefix+]"
type = "shell"
command = "herdr-hunk --tab"

[[keys.command]]
key = "prefix+alt+g"
type = "shell"
command = "herdr-worktree-layout"

# Keep this fallback: prefix+alt+g may be swallowed by terminal/tmux on macOS.
[[keys.command]]
key = "prefix+ctrl+g"
type = "shell"
command = "herdr-worktree-layout"

[worktrees]
directory = "~/.local/share/herdr/worktrees"
