# Sesh configuration (nix-managed)
# https://github.com/joshmedeski/sesh

# Default session settings
[default_session]
# startup_command = "nvim"  # uncomment to start nvim in new sessions

# Session-specific configurations
# [[session]]
# name = "dotfiles"
# path = "~/.config/dotfiles"
# startup_command = "nvim"

# Blacklist sessions from appearing in the list
# blacklist = ["scratch", "popup"]
