# Agent Instructions

This directory stores a snapshot of Edmund's current Zed settings.

Rules:

- Do not wire these files into Nix or Home Manager unless explicitly asked.
- Treat `settings.json` as a parked reference copy of the live local config.
- If asked to refresh it, copy from the live file at `~/.config/zed/settings.json`.
- Keep edits small and preserve user comments when possible.
