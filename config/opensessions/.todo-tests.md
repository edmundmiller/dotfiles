# TODO tests: opensessions Pi watcher

This markdown TODO list is kept alongside the executable Bun `test.todo(...)` items in:

- `config/opensessions/tests/pi-watcher.test.js`

## Outstanding test coverage

- [ ] Partial-line JSON append recovery
- [ ] Recursive scan depth > 1
- [ ] File truncation / rewrite handling
- [ ] Duplicate suppression when `fs.watch` and polling overlap
- [ ] Real end-to-end opensessions sidebar runtime validation
