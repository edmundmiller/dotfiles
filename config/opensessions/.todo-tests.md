# TODO tests: opensessions Pi watcher

## Next test cases to add

- [ ] Partial-line JSON append recovery
  - Repro: write one JSON line in two chunks, ensure final completed line is processed once.
  - Expected: no missed status transition after second chunk arrives.

- [ ] Recursive scan depth > 1
  - Repro: place transcript at `sessions/<project>/<nested>/thread.jsonl`.
  - Expected: watcher discovers and processes nested files at arbitrary depth.

- [ ] File truncation / rewrite handling
  - Repro: process a file, truncate it, then append new events.
  - Expected: watcher resets offset safely and emits transitions from new content only.

- [ ] Rename + move handling
  - Repro: append events, rename/move the transcript file, continue writing.
  - Expected: thread identity and status tracking remain correct.

- [ ] Duplicate event suppression with fs.watch + polling overlap
  - Repro: trigger updates while both watchers are active.
  - Expected: single logical transition emitted per state change.

- [ ] Real opensessions runtime integration (non-synthetic)
  - Repro: start opensessions server, run real Pi session in mapped project.
  - Expected: sidebar status and thread label update correctly end-to-end.

- [ ] Multi-thread same-project behavior
  - Repro: two Pi transcripts in same project update concurrently.
  - Expected: latest active thread updates session status predictably.

- [ ] Large transcript performance sanity
  - Repro: preload large JSONL (100MB-class), then append incremental events.
  - Expected: incremental processing remains responsive, no full reread on append.

- [ ] Unknown/novel event-shape resilience
  - Repro: inject unexpected `customType` and malformed partial objects.
  - Expected: watcher ignores safely without crashing or status corruption.

## Current known gaps (from local probing)

- Partial-line append can miss a transition (line buffering needed).
- Scan currently only descends one directory level.
