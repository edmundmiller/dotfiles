---
description: "Frame a task with an outcome, stopping condition, and proof, then work on it."
argument-hint: "<TASK>"
---

Briefly frame the request below, then work toward its requested stopping point.

Request:

$ARGUMENTS

State:

- `Outcome`: the requested end state.
- `Done when`: the observable stopping condition.
- `Proof`: the checks or artifacts that establish it.

Inspect repository facts and ask only when intent or authority is missing.
Stay within the request: planning does not authorize implementation or publication.
Use the available tools; no persistent goal service or background continuation is required.
Verify the result before claiming completion. If blocked, state the evidence,
what remains, and the smallest action needed to continue.
