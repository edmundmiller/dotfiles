#!/usr/bin/env bash
# Compatibility entry for package publication; selection and execution live in
# the shared validator. Prefer hey check for everyday work.
set -euo pipefail
root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "$root/scripts/validation.py" --packages-only "$@"
