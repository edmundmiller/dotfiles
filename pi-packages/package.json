{
  "private": true,
  "workspaces": [
    "pi-*"
  ],
  "devDependencies": {
    "@marcfargas/pi-test-harness": "^0.5.0",
    "@types/bun": "^1.3.9",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.18"
  },
  "scripts": {
    "typecheck": "bun --filter '*' typecheck",
    "check": "bun --filter '*' typecheck",
    "test:integration": "npx vitest run --config vitest.config.ts"
  }
}
