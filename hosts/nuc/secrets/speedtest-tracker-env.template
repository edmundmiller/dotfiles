# Speedtest Tracker environment variables
# Copy to a temp file, fill in values, then encrypt:
#   agenix -e hosts/nuc/secrets/speedtest-tracker-env.age
#
# Generate APP_KEY: openssl rand -base64 32
APP_KEY=base64:
