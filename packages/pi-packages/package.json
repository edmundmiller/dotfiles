{
  "private": true,
  "workspaces": [
    "pi-*"
  ],
  "devDependencies": {
    "@marcfargas/pi-test-harness": "^0.5.0",
    "@types/bun": "^1.3.9",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "typecheck": "bun --filter '*' typecheck",
    "check": "bun --filter '*' typecheck",
    "test:integration": "bun test ./tests/*.test.ts --timeout 30000"
  }
}
