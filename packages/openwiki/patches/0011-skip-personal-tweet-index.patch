diff --git a/src/okf/index-sync.ts b/src/okf/index-sync.ts
index 5760d75..e2355bf 100644
--- a/src/okf/index-sync.ts
+++ b/src/okf/index-sync.ts
@@ -20,6 +20,7 @@ const EXCLUDED_FILES = new Set([
   "AGENTS.md",
   "INSTRUCTIONS.md",
 ]);
+const EXCLUDED_INDEX_DIRECTORIES = new Set(["Clippings/Tweets"]);
 
 /**
  * A wiki directory paired with the entries it directly contains.
@@ -67,6 +68,13 @@ export async function synchronizeWikiIndexes(
 ): Promise<void> {
   const root = outputMode === "local-wiki" ? "/" : "/openwiki";
   for (const directory of await collectDirectories(backend, root, true)) {
+    if (
+      EXCLUDED_INDEX_DIRECTORIES.has(
+        path.posix.relative(root, directory.path),
+      )
+    ) {
+      continue;
+    }
     await synchronizeDirectory(backend, directory, root, labels, conceptType);
   }
 }
diff --git a/test/index-middleware.test.ts b/test/index-middleware.test.ts
index f063097..f2cc42c 100644
--- a/test/index-middleware.test.ts
+++ b/test/index-middleware.test.ts
@@ -316,6 +316,20 @@ describe("synchronizeWikiIndexes", () => {
     ).resolves.not.toContain("AGENTS.md");
   });
 
+  test("does not create a redundant index for personal tweet clippings", async () => {
+    const { backend, rootDir } = await setup("local-wiki");
+    await backend.write(
+      "/Clippings/Tweets/tweet.md",
+      document("Tweet", "Imported tweet clipping."),
+    );
+
+    await synchronizeWikiIndexes(backend, "local-wiki");
+
+    await expect(
+      readFile(path.join(rootDir, "Clippings/Tweets/index.md"), "utf8"),
+    ).rejects.toMatchObject({ code: "ENOENT" });
+  });
+
   test("supports the local wiki root and empty directories", async () => {
     const { backend, rootDir } = await setup("local-wiki");
     await backend.write(
