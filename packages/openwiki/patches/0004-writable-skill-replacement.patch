diff --git a/src/agent/skills.ts b/src/agent/skills.ts
index d643059..ba473a5 100644
--- a/src/agent/skills.ts
+++ b/src/agent/skills.ts
@@ -1,5 +1,7 @@
 import {
+  chmod,
   cp,
+  lstat,
   mkdir,
   mkdtemp,
   readdir,
@@ -21,6 +23,36 @@ export async function syncBundledSkills(): Promise<void> {
   await replaceSkillDirectories(bundledSkillsDir, openWikiSkillsDir);
 }
 
+async function makeDirectoryTreeWritable(directory: string): Promise<void> {
+  let stats;
+  try {
+    stats = await lstat(directory);
+  } catch (error) {
+    if (
+      error instanceof Error &&
+      "code" in error &&
+      error.code === "ENOENT"
+    ) {
+      return;
+    }
+    throw error;
+  }
+
+  if (!stats.isDirectory()) {
+    return;
+  }
+
+  await chmod(directory, (stats.mode & 0o777) | 0o700);
+  const entries = await readdir(directory, { withFileTypes: true });
+  await Promise.all(
+    entries
+      .filter((entry) => entry.isDirectory())
+      .map((entry) =>
+        makeDirectoryTreeWritable(path.join(directory, entry.name)),
+      ),
+  );
+}
+
 /** Replaces bundled skill directories without removing unrelated skills. */
 export async function replaceSkillDirectories(
   sourceDir: string,
@@ -57,6 +89,7 @@ async function replaceSkillDirectory(
 
   try {
     await cp(path.join(sourceDir, name), staged, { recursive: true });
+    await makeDirectoryTreeWritable(target);
 
     // Move any existing copy aside; it is deleted with the scratch directory.
     try {
diff --git a/test/skills.test.ts b/test/skills.test.ts
index f6ce5f0..907ab2d 100644
--- a/test/skills.test.ts
+++ b/test/skills.test.ts
@@ -1,10 +1,12 @@
 import {
+  chmod,
   mkdir,
   mkdtemp,
   readdir,
   readFile,
   rm,
   stat,
+  symlink,
   writeFile,
 } from "node:fs/promises";
 import os from "node:os";
@@ -107,7 +109,7 @@ describe("replaceSkillDirectories", () => {
     expect(normalizedSkill).toContain("openwiki: mermaid parse failed");
   });
 
-  test.fails("replaces skills copied from a read-only package", async () => {
+  test("replaces skills copied from a read-only package", async () => {
     const root = await mkdtemp(path.join(os.tmpdir(), "openwiki-skills-"));
     const source = path.join(root, "source");
     const target = path.join(root, "target");
@@ -132,4 +134,28 @@ describe("replaceSkillDirectories", () => {
       await rm(root, { force: true, recursive: true });
     }
   });
+
+  test("does not follow target symlinks", async () => {
+    const root = await mkdtemp(path.join(os.tmpdir(), "openwiki-skills-"));
+    const source = path.join(root, "source");
+    const target = path.join(root, "target");
+    const sourceSkill = path.join(source, "bundled");
+    const outside = path.join(root, "outside");
+
+    try {
+      await mkdir(sourceSkill, { recursive: true });
+      await mkdir(target);
+      await mkdir(outside);
+      await writeFile(path.join(sourceSkill, "SKILL.md"), "packaged");
+      await chmod(outside, 0o555);
+      await symlink(outside, path.join(target, "bundled"));
+
+      await replaceSkillDirectories(source, target);
+
+      expect((await stat(outside)).mode & 0o777).toBe(0o555);
+    } finally {
+      await chmod(outside, 0o755).catch(() => undefined);
+      await rm(root, { force: true, recursive: true });
+    }
+  });
 });
