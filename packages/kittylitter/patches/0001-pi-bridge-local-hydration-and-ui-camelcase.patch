diff --git i/crates/pi-bridge/src/bridge.rs w/crates/pi-bridge/src/bridge.rs
index 9f064e1..592ece1 100644
--- i/crates/pi-bridge/src/bridge.rs
+++ w/crates/pi-bridge/src/bridge.rs
@@ -207,15 +207,18 @@ impl PiBridgeBuilder {
 
         let hydrator = match self.hydrator {
             Some(hydrator) => hydrator,
+            // Local daemon mode can read ~/.pi/agent/sessions directly. Avoid
+            // spawning pi during startup to call the speculative list_sessions
+            // RPC, which is not part of released pi-coding-agent builds.
+            None if !self.rpc_session_listing_only => PiHydrator::new(),
+            // Remote launchers cannot safely scan the caller's local ~/.pi, so
+            // keep the RPC-only behavior there and degrade to an empty index if
+            // the remote pi build does not expose list_sessions.
             None => match list_sessions_via_rpc(&pool).await {
                 Ok(sessions) => PiHydrator::with_sessions(sessions),
                 Err(error) => {
                     tracing::warn!(%error, "pi RPC list_sessions failed during bridge startup");
-                    if self.rpc_session_listing_only {
-                        PiHydrator::with_sessions(Vec::new())
-                    } else {
-                        PiHydrator::new()
-                    }
+                    PiHydrator::with_sessions(Vec::new())
                 }
             },
         };
diff --git i/crates/pi-bridge/src/pool/pi_protocol.rs w/crates/pi-bridge/src/pool/pi_protocol.rs
index 33f9e13..8a69a54 100644
--- i/crates/pi-bridge/src/pool/pi_protocol.rs
+++ w/crates/pi-bridge/src/pool/pi_protocol.rs
@@ -683,7 +683,7 @@ pub enum CompactionReason {
 
 /// `extension_ui_request` body without the outer `type` discriminator.
 #[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]
-#[serde(tag = "method", rename_all = "snake_case")]
+#[serde(tag = "method", rename_all = "camelCase")]
 pub enum ExtensionUiRequest {
     Select {
         id: String,
diff --git i/crates/alleycat/src/agent_manifest.rs w/crates/alleycat/src/agent_manifest.rs
--- i/crates/alleycat/src/agent_manifest.rs
+++ w/crates/alleycat/src/agent_manifest.rs
@@ -98,7 +98,7 @@ pub const MANIFESTS: &[AgentManifest] = &[
         aliases: &["ampcode", "amp-code", "amp_code", "amp code"],
         locks_reasoning_effort_after_activity: true,
         visible_modes: Some(&["smart", "rush", "deep"]),
-        supports_ssh_bridge: false,
+        supports_ssh_bridge: true,
         uses_direct_codex_port: false,
         supports_thread_permission_overrides: false,
         reports_effective_thread_permissions: false,
@@ -146,7 +146,7 @@ pub const MANIFESTS: &[AgentManifest] = &[
         aliases: &["factory", "factory-droid", "factory_droid", "factory droid"],
         locks_reasoning_effort_after_activity: false,
         visible_modes: None,
-        supports_ssh_bridge: false,
+        supports_ssh_bridge: true,
         uses_direct_codex_port: false,
         supports_thread_permission_overrides: false,
         reports_effective_thread_permissions: false,
@@ -162,7 +162,7 @@ pub const MANIFESTS: &[AgentManifest] = &[
         aliases: &[],
         locks_reasoning_effort_after_activity: false,
         visible_modes: None,
-        supports_ssh_bridge: false,
+        supports_ssh_bridge: true,
         uses_direct_codex_port: false,
         supports_thread_permission_overrides: false,
         reports_effective_thread_permissions: false,
diff --git i/crates/alleycat/src/agents.rs w/crates/alleycat/src/agents.rs
--- i/crates/alleycat/src/agents.rs
+++ w/crates/alleycat/src/agents.rs
@@ -914,10 +914,7 @@ impl AgentManager {
 
     fn droid_available(&self) -> bool {
         let cfg = self.config.load();
-        cfg.agents.droid.enabled
-            && which::which(&cfg.agents.droid.bin).is_ok()
-            && has_factory_auth(&cfg.agents.droid.api_key_env)
+        cfg.agents.droid.enabled && which::which(&cfg.agents.droid.bin).is_ok()
     }
 
     fn devin_available(&self) -> bool {
