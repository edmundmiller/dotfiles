# Database
*.db
*.db-journal
*.db-shm
*.db-wal

# Lock files
*.lock
.write.lock

# Temporary
last-touched
.local_version
*.tmp

# Local history backups
.br_history/

# Legacy bd backup exports
backup/

# DB-family recovery artifacts
.br_recovery/

# Sync state (local-only, per-machine)
.sync.lock
sync_base.jsonl

# Merge artifacts (temporary files from 3-way merge)
beads.base.jsonl
beads.base.meta.json
beads.left.jsonl
beads.left.meta.json
beads.right.jsonl
beads.right.meta.json

# Daemon/runtime files
bd.sock
daemon.lock
daemon.log
daemon.pid
sync-state.json

# Worktree redirect file
redirect

# bv (beads viewer) lock file
.bv.lock
