---
purpose: Generated inventory of agent quality gate capabilities.
applies_to: Agent finish, audit, and capability discovery workflows.
entrypoint: Edit .agents/quality.json, then run python3 bin/agent-quality inventory.
verification: python3 bin/agent-quality inventory --check
update_when: .agents/quality.json checks or inventory generator change.
---

# Agent quality capabilities

Generated by `bin/agent-quality inventory`. Edit `.agents/quality.json`, not this file.

## repo-quality

- Kind: full-validation
- Applies to: `always`
- Command: `./bin/hey check --worktree`
- Purpose: Darwin evaluation, formatting, hooks, and shell tests

## agent-quality-tests

- Kind: test
- Applies to: `always`
- Command: `python3 -m unittest tests/test_agent_quality.py tests/test_agent_rules.py tests/test_skill_quality_validator.py tests/test_agent_instruction_wiring.py`
- Purpose: Agent workflow engine behavior

## agent-instructions

- Kind: lint
- Applies to: `always`
- Command: `python3 bin/check-agent-rules && python3 skills/catalog/skill-quality/scripts/validate.py skills/catalog .agents/skills skills/conditional`
- Purpose: Shared rule metadata and portable skill structure

## test-confidence

- Kind: test-audit
- Applies to: `always`
- Command: `python3 bin/agent-quality audit-tests tests packages/pi-packages`
- Purpose: Vacuous and skipped test heuristics

## inventory-drift

- Kind: documentation
- Applies to: `always`
- Command: `python3 bin/agent-quality inventory --check`
- Purpose: Generated capability inventory matches its manifest

## visual-regression

- Kind: visual
- Applies to: `packages/*/screenshots/**, tests/visual/**`
- Command: `python3 bin/agent-quality visual-compare tests/visual/baseline tests/visual/current`
- Purpose: Screenshot baseline comparison for declared UI surfaces

## zsh-performance

- Kind: performance
- Applies to: `config/zsh/**, modules/shell/zsh/**, bin/zbench*`
- Command: `./bin/hey zbench-check`
- Purpose: Zsh startup performance regression threshold

## Operating rules

- `PASS` means the command ran and succeeded.
- `FAIL` means the command ran and failed.
- `NOT_APPLICABLE` means changed paths did not select the check; it is never a pass.
- `SKIP` means a required prerequisite was unavailable; it is never a pass.
- Add subsystem checks to the manifest with narrow path globs and non-interactive commands.
